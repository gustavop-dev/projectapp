interface:
  display_name: "View Map Audit"
  short_description: "Auditoría del Mapa de vistas (`/panel/views`) — verifica que..."
  default_prompt: "Use $view-map-audit to handle this task with its documented workflow."

policy:
  allow_implicit_invocation: true
