# ===========================================
# .env_development — host-only network dev profile (frontend)
# ===========================================
# Loaded only when Nuxt is started with `--dotenv .env_development`.
# Default `.env` and production builds ignore this file.
# Gitignored (matches `*.env`).

# Bind Nuxt dev server to all interfaces so the host can reach 192.168.56.10
NUXT_HOST=0.0.0.0
NUXT_PORT=3000

# Public reCAPTCHA site key (copied from .env — Nuxt only auto-loads one dotenv)
NUXT_PUBLIC_RECAPTCHA_SITE_KEY=6Lf5uZksAAAAAMy0a5u0hqf2hpssMZkjpEOHd2bq
