{
  "id": "admin-proposal-resend",
  "name": "Admin Proposal Resend",
  "module": "admin",
  "roles": [
    "admin"
  ],
  "priority": "P2",
  "description": "Resend an already-sent proposal via 'Re-enviar' from the actions modal. Confirmation dialog → POST /proposals/:id/resend/ keeps existing expires_at, resets sent_at and reminder timers, and returns email_delivery. The panel toast surfaces success or failure with the reason.",
  "expectedSpecs": 1,
  "outcomes": [
    "success",
    "failure"
  ]
}
