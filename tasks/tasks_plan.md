# Task Plan — ProjectApp

> **Calendario contable listo para integrar — 2026-09-01:** se auditó el calendario ya
> implementado y se fijaron como contrato los hitos 15/7/día, la recurrencia
> quincenal por defecto, la pausa individual visible y los destinatarios
> exclusivamente internos. La aceptación añade cobertura de digest, cadencia,
> contenido, logging por target, fallo de entrega y el flujo P1 completo de
> silenciar/reactivar; además corrige el nombre canónico del cliente sin cambiar
> esquema ni crear historial de pagos recurrentes. Verificación focal: 24
> pruebas backend, 35 unitarias, 6 E2E sin retries, mapa fresco y quality gate
> sin errores nuevos; auditoría final: 0 tests basura.

> **Aceptación lista para integrar — 2026-09-01:** los enlaces entre la ficha
> comercial y el espacio de proyecto ya estaban completos; la entrega endurece
> su aceptación E2E sin tocar producto. Nuxt tiene readiness explícita y warmup
> autenticado de rutas críticas, y hosting, ingreso y cuenta de cobro cuentan
> con escenarios funcionales propios. Repeticiones, regresión de 10 casos y
> quality gate focal están verdes.

> **Refinamiento implementado — 2026-09-01:** la reacción transversal de iconos
> usa un pulso reiniciable de 360 ms con compresión, rebote y halo expansivo.
> Reduced motion conserva un halo estático; los resultados verificables y los
> estados async mantienen el contrato previo.

> **Listo para integrar — 2026-09-01:** las acciones de cada estado de Proyecto
> quedan divididas en una banda de edición/guardado y otra de fusión/retiro, con
> proporciones consistentes y separación responsive en cinco viewports. El
> catálogo de Documentos y la validación inline previa permanecen intactos.

> **Listo para integrar — 2026-09-01:** la acción de fila de Clientes dice
> **Archivar** o **Desarchivar** y usa el catálogo semántico correspondiente.
> Conserva el mecanismo existente de Archivados y no modifica **Desactivar
> acceso** en la plataforma. Dos unitarias, cuatro E2E y el guard del panel están
> verdes.
> **Follow-up de Módulos adicionales listo para integrar — 2026-09-01:** el
> catálogo público ya no muestra el Navbar flotante; enlaces y PDF parten en
> español con cambio explícito a inglés. La sesión staff queda excluida, sin
> registro paralelo, de todo tracking comercial en catálogo, propuestas y
> diagnósticos; el histórico permanece intacto y una apertura anónima sigue
> contando. Verificación focal: 14 pruebas backend, 12 unitarias frontend, 4
> escenarios E2E y checks de Django/diseño en verde; no requiere migración.

> **Listo para integrar — 2026-08-31:** el catálogo de estados de Proyectos
> reemplaza las listas rojas por requisitos y errores bajo cada campo después
> del intento. La fusión distingue la selección faltante, que se corrige inline,
> de la restricción permanente de estados semilla, que conserva ayuda accesible.

> **Listo para integrar — 2026-08-31:** todos los iconos interactivos de panel,
> plataforma y vistas públicas comparten reacción inmediata; copiar confirma el
> resultado real y diferencia éxito/fallo. El contrato queda protegido por el
> design system, un composable, CI, unitarias y el flow E2E transversal.

> **Listo para integrar — 2026-08-31:** ambos grupos de indicadores de Proyectos
> comparten tarjetas horizontales de 80 px y grilla de cuatro/cinco columnas en
> escritorio; el apoyo queda acotado y la ayuda permanece dentro de cada tarjeta.
> En celular/tableta se conservan los dos resúmenes con drawers completos.

> **Acceso comercial listo para integrar — 2026-08-31:** Módulos adicionales
> muestra la URL pública y la descarga completa en PDF al inicio del módulo y
> desde el dashboard. Las selecciones personalizadas y su seguimiento permanecen
> separados y el resumen destaca enlaces vigentes sin abrir y la última visita.

> **Listo para integrar — 2026-08-30:** el Gestor Documental expone el catálogo
> completo de proyectos y clientes, separa activos de archivados y reserva
> **Carpetas propias** para raíces realmente no asignadas. PRUEBA queda activo y
> Candle archivado por ciclo de vida. `managed_project` identifica la única raíz
> canónica y la adopción histórica se entrega como manifiesto v5 revisable con
> respaldo, huella e inverso; no se modificó producción.

> **Listo para integrar — 2026-08-30:** Comunicaciones incorpora un centro
> personal de Configuraciones junto a Nuevo hilo, persistencia por cuenta,
> migración única de preferencias locales, guard de cambios y restablecimientos
> independientes. Verificación: 15 pruebas backend, 42 unitarias frontend, 13 E2E,
> build de producción, checks de contrato y auditoría del flujo en verde.

> **Ampliación lista para integrar — 2026-08-30:** Módulos adicionales suma
> cinco opciones al catálogo existente, incorpora selector ES/EN en panel,
> público y enlaces compartidos, ofrece tarjetas/lista/acordeón y genera PDF en
> el idioma activo con destinatario opcional desde el panel.

> **Listo para integrar — 2026-08-30:** Proyectos elimina Pausado y consolida
> catálogo, estado compatible, episodios e impactos financieros en Suspendido,
> única detención reversible.

> **Listo para integrar — 2026-08-29:** Nuevo proyecto y los modales de creación
> del panel comparten validación bajo el campo, anchos coherentes, textos breves,
> una sola convención de obligatoriedad y una fila final sólo para acciones.

> **Supersedido — 2026-08-29:** el diagnóstico técnico de conciliación permanece
> disponible para operación, pero ya no se muestra en el sidebar. El contrato
> vigente es el catálogo completo y el manifiesto v5 descritos arriba.

> **Listo para integrar — 2026-08-30:** Comunicaciones limita **Asunto** al
> título del hilo en tabla y tarjetas, mantiene el contenido completo en el
> detalle y conserva el orden activo. Unitarias, E2E, tokens y flow audit están
> verdes.

> **Segundo hotfix listo para integrar — 2026-08-29:** la recuperación segura
> del residuo vacío de `content.0223` se ejecuta fuera de
> `transaction.atomic()`, como exige el DDL no transaccional de MySQL. El primer
> hotfix de índices ya está en `main`; producción espera este merge y redeploy.

> **Listo para integrar — 2026-08-29:** tracking de propuestas pausado en
> pestañas ocultas, con cierre único, reanudación segmentada y flush exclusivo.

## 1. Feature Status

| Feature | Status | Details |
|---------|--------|---------|
| Projects — platform link acceptance | 🟡 Ready for merge | 2026-09-01. La auditoría confirmó el enlace bidireccional existente mediante el `Project.id` canónico, puente de sesión en pestaña nueva y retorno comercial sólo para administradores. La aceptación espera el montaje de Nuxt y calienta rutas privadas localizadas con identidad simulada; un spec nuevo recorre las referencias desde hosting, ingreso y cuenta de cobro. No cambia producto, API ni esquema. Tres casos inestables y tres nuevos pasaron dos veces, la regresión íntegra de 10 casos quedó verde y el quality gate focal cerró en 93/100 sin errores. |
| Projects — proportional state-catalog actions | 🟡 Ready for merge | 2026-09-01. Active rows in `/panel/projects/statuses` use separate edit/save and merge/retire bands. Save, Merge and Retire are full-width on compact screens; portrait uses a full-row Save plus a 6/3/3 maintenance grid; landscape and wider screens use a 3/2/3/2/2 edit grid plus an 8/2/2 maintenance grid. Gaps stay at least 8 px, Documents keep their previous layout, and PA-120 field feedback is preserved. Twelve focused unit tests, five responsive E2E scenarios, three action regressions, the design-token guard and the Nuxt build are green. |
| Clients — archive row action semantics | 🟡 Ready for merge | 2026-09-01. `/panel/clients` uses the canonical `archive`/`restore` actions and displays the visible tooltips **Archivar**/**Desarchivar**. The existing preview, confirmation, cascade, archived filter and API contract stay unchanged; platform access deactivation remains a separate action. Two focused unit tests, all four archived-client E2E scenarios and the panel action guard pass. |
| Projects — inline state-catalog validation | 🟡 Ready for merge | 2026-08-31. Create/edit/merge validation in `/panel/projects/statuses` uses `BaseFormField` beside the relevant control after an attempted action; required markers cover name, description and operational effect. Serializer field errors stay attached to their field and clear on input. A missing merge target is actionable inline feedback, while immutable seed restrictions remain accessible help on the disabled action. Documents retain their existing catalog behavior. Six unit tests, all 14 affected E2E scenarios, the disabled-control guard, Nuxt build and flow audit are green. |
| Transversal — reacción visible de iconos interactivos | 🟡 Ready for merge | Refined 2026-09-01. Todos los controles icon-only ejecutables comparten un pulso reiniciable de 360 ms (90% → 105% → 100%) y halo expansivo; reduced motion conserva sólo el halo estático. Copiar confirma únicamente el resultado real, el glifo permanece estable y async conserva loading. QA aprobada: 25 unitarias focales, 3 E2E sin retries, build Nuxt, guards, flow audit y quality gates en verde. |
| Documents — safe project/client reconciliation | 🟡 Ready for merge | Updated 2026-08-30. Every canonical project appears even with zero content; a visit-local toggle reveals non-operational projects inclusively while the separate archive toggle still filters content. `DocumentFolder.managed_project` identifies the canonical root. Manifest v5 promotes G&M, Vástago, Xpandia, Kore and Tenndalux, creates roots for Mimittos, PRUEBA and Candle, nests Germán under Kore, files document 120 and explicitly assigns loose documents 1–5/135/157/154/159. Carlos, Gustavo, Aarón, Littigio, ProjectApp and Requirement Estimates stay untouched. Apply requires a reviewed hash, unchanged fingerprint, verified backup and inverse snapshot. Production remains untouched pending deploy and the operator runbook. |
| Projects — compact indicator cards | 🟡 Ready for merge | Updated 2026-08-31. Expanded non-zero lifecycle and operational indicators share `BaseIndicatorCard`'s 80 px horizontal layout, the same four/five-column grid, bounded support, inline result/action and a dedicated 48 px help column. At 412/835 px, exactly two equal summaries retain all facts in drawers. Nineteen unit tests, 17 functional scenarios and geometry at 412/835/1195/1440/2560 are green; the flow map is registered. |
| Documents — project/client navigation switch | 🟡 Ready for merge | 2026-08-29, refined 2026-08-30. The document manager reuses Communications' shared Project/Client segmented control, persists the choice per staff profile and exposes the selected mode through `by` in the URL. A constant-query backend facet service counts every directly associated folder and document once and returns complete catalogs, including zero-content rows and permanent Sin proyecto/Sin cliente buckets. Projects default to operational rows; a non-persisted inclusive toggle reveals non-active rows and resets hidden selections. The global content archive toggle remains independent beside **Carpetas propias**, which contains only roots with neither entity. The technical PA-108 warning is no longer exposed in the UI. |
| Panel — creation-form validation and modal actions | 🟡 Ready for merge | 2026-08-29. Nuevo proyecto muestra nombre/cliente/estado/descripción como un bloque de ancho completo, conserva En desarrollo sin ayuda contradictoria y expone Crear cliente aun con coincidencias. Errores locales y del API quedan bajo su control con relación ARIA; Cancelar/Guardar quedan solos. `BaseFormField`, `ClientAutocomplete` y los modales de creación de Clientes, Tareas, Documentos, Contabilidad y contenido comparten el contrato; no hay cambio backend ni de esquema. Unitarias focales, cuatro escenarios Playwright, build y flow audit están verdes. |
| Proposals — visibility-aware engagement tracking | 🟡 Ready for merge | 2026-08-29. Ocultar la página finaliza un único segmento por beacon y detiene el intervalo; volver a verla inicia otro segmento. Los flushes concurrentes se coalescen y un response sólo limpia las entradas que realmente envió. 38 pruebas focales verdes. |
| Emails — historial probatorio con adjuntos exactos | 🟡 Second hotfix ready for merge | Actualizado 2026-08-29. El primer hotfix de MySQL ya está en `main`: conserva la URL completa, usa `url_sha256` para la unicidad y hace que `content.0223` reconozca sólo el residuo vacío. El segundo deploy mostró que Django envolvía esa recuperación en una transacción incompatible con DDL MySQL. La operación queda explícitamente `atomic=False`, con una regresión sobre el límite transaccional; el fallo ocurrió antes de limpiar y la base conserva el mismo residuo vacío. Producción espera merge + redeploy. |
| Commercial — additional modules catalog | 🟡 Ready for merge | Updated 2026-08-31. Dominio administrable con 23 módulos bilingües en cinco categorías. El módulo y el dashboard muestran un centro comercial con URL canónica copiable/abrible, PDF completo en un clic, creación de selecciones, PDF personalizado y seguimiento. El catálogo canónico permanece indexable y sin tracking; cada selección es `noindex`, fija módulos y registra aperturas. El dashboard resume módulos activos, enlaces vigentes, pendientes de apertura y la última visita histórica. Panel, público y enlaces conservan selector ES/EN y tarjetas/lista/acordeón; no hay precios, cambios de esquema ni MCP. El flow nuevo y las regresiones existentes están verdes sin `junk-only`. |
| Communications — subject-only thread index | 🟡 Ready for merge | Updated 2026-08-30. Both the landscape table and compact cards render only `thread.title` in the identity surface; message bodies stay in the URL-addressed detail modal. Client/project, channel, state, count, recency, drafts and the persisted order remain unchanged, as do the REST DTO and content search. Seven component tests and all 13 communications E2E scenarios pass; design tokens are clean, the flow map is fresh and the audit reports 0 junk-only / 0 missing. |
| Communications — navigation, search, saved views and factory filters | 🟡 Ready for merge | Updated 2026-08-30. The base delivery merged in #306 and project-name search landed in #311. `/panel/communications` combines the resizable project/client navigator, explicit **Sin proyecto** scope and URL-addressed thread workspace with the shared filter strip. Six factory cuts start with **Borradores pendientes**, always show counts including zero, and add the open-only **Enviados sin respuesta** reply cut; own views are labelled **Propia**, coexist in the same reorderable/overflow-safe strip and survive factory reset. REST/MCP share project-aware text search plus the correlated `reply_status` query contract, while a bounded full-dataset endpoint supplies tab counts. The factual manual-workflow notice remains dismissible. Verification: 20 focused backend tests, 32 frontend-unit tests, 8 E2E scenarios, production build and four-outcome flow audit green. |
| Documents — automatic filing for generated records | 🟡 Ready for merge | 2026-08-28. Issued collection accounts now create/reuse a system-owned Project or Client/Sin proyecto → Cuentas de cobro → Year → Month hierarchy from the Bogotá issue date; cancellations move to Anuladas and cancelled drafts to Sin emitir/Anuladas. Sent and resent commercial proposals retain the exact attached PDF as immutable vNN snapshots under the parallel Propuestas comerciales hierarchy, and acceptance moves every version to the new project. Derived account delivery states replace Por clasificar, generated branches reject manual structural/content mutations in REST and MCP, titles sort usefully, fake data follows the live rule, and a dry-run-first command backfills only folderless historical accounts. Focused result: 45 backend service/view/backfill cases, 1 fake-data contract, 13 frontend unit cases, 3 Playwright outcomes, historical send/resend regressions, migration checks and the production build are green. |
| Panel — single readable action tooltip | 🟡 Ready for merge | Updated 2026-08-30. `BaseActionButton` owns one short hover/focus hint while contextual row text remains only in the accessible name. The action tooltip explicitly uses `white-space: nowrap`, remains clamped to the viewport and no native `title` competes with it; longer descriptive `BaseTooltip` consumers keep their multiline wrapping. Unit and Playwright geometry checks verify horizontal readability and viewport containment without changing click/touch behavior. |
| Proposals — contextual cross-cutting features card | 🟡 Ready for merge | 2026-08-28. Functional requirements now expose a fourth core card, `cross_cutting_features`, immediately after specific features in ES/EN. Its seven starter quality items are editable and prompts must adapt them to the business, product stage, audience and real scope without duplication or unsupported guarantees. Responsive design moved from `features`; stable item ids feed technical epics, PDF links and project scope sync. Migration `0222` updates default snapshots and active drafts only. Focused backend/unit/E2E coverage, flow freshness and coverage audit are green. |
| Projects/Accounting — aligned responsive indicator headers | 🟡 Ready for merge | 2026-08-28. `BaseIndicatorCard` fixes label/value/support geometry and standardizes help plus explicit action. Projects groups non-zero lifecycle states in catalog order apart from operational pending facts; Incomes keeps four ranked expanded cards. At 412/835 both pages render exactly two drawer-backed summaries without losing zero states or operational details, and the first list row stays in the phone viewport. Focused unit/E2E coverage spans all five reference widths and filter/navigation actions; flow registry and Nuxt build are part of the delivery gate. |
| Incident — Nuxt SPA fallback and panel availability | 🟡 Ready for merge | 2026-08-28. The Nuxt/i18n upgrade generated a 101-byte `200.html` that meta-refreshed to `/en-us/200.html`; Django served the same fallback again, so every non-prerendered panel route looped while health checks remained green. Browser-language detection is disabled because Django owns root locale selection, and `build:django` now rejects empty, redirecting or mount-less fallbacks before its atomic swap. Four Jest validator cases, three Django route regressions, direct generation and the full publication build pass. Production remains pending merge/deploy; add a content-aware deep-route probe so API-only health cannot miss this failure class. |
| Accounting — Pocket ledger cards on mobile | 🟡 Ready for merge | 2026-08-28. Below 640 px Bolsillo now renders one complete card per movement with leading 44 px menu, concept, signed amount, date, atomic direction/link state and Saldo después/Acumulado filtrado; sorting remains available and horizontal scroll disappears. From 640 px the priority table remains. Ingresos and Cuentas de cobro now share the leading menu and atomic badges in classic/grouped views. PA-103 focal trace records 412 plus the four larger Pocket profiles and explicitly leaves the full 60-cell walkthrough pending. Focused result: 13 unit, 18 responsive/E2E cases and affected flows complete with no junk-only. |
| Backend — DocumentState system-key integrity on MySQL | 🟡 Ready for merge | 2026-08-27. The redundant conditional predicate was removed from `(catalog, system_key)`, so MySQL now receives the same uniqueness invariant as SQLite/PostgreSQL while multiple `NULL` custom keys remain valid. Migration `0218` fails before DDL if production contains duplicates. `sqlmigrate`, Django database checks, 4 constraint tests and all 19 MCP contract cases pass. |
| Accounting — recurring lifecycle and row actions | 🟡 Ready for merge | 2026-08-27. Grouped/classic rows now use a PA-102 leading menu for edit, duplicate-as-draft, active/inactive state, reminder mute and archive. Current/archived scopes, inactive-only restore, archive-gated typed hard delete and atomic bulk activate/deactivate/archive are enforced by one audited REST/MCP service. Only active non-archived rows feed totals, weights, charts and notices. Fake data and six MCP tools carry parity. Focused verification: 34 backend, 62 frontend-unit and 11 E2E cases; migration/check, panel gates, flow audit and Nuxt build pass. Charge registration and charge history remain a separate ledger-origin follow-up. |
| Panel — atomic short controls, semantic modal widths and Gestor Documental | 🟡 Ready for merge | 2026-08-27. A pre-fix sweep found 61 modal surfaces, 32 segmented consumers, 25 badges and 19 shared form rows. Short button/badge/segmented labels now keep text, icons and four-digit counts indivisible while groups reflow between options. Modal kinds standardize 28/42/64/80/64/100-rem ceilings with the existing narrow-screen contract. Shared label/control/error bands, group-level help and a control-aligned action cell close row drift in Client, Collection Account and Documents cases. The public module label is Gestor Documental across navigation, view taxonomy, flows and MCP data while `/panel/documents`, models and slugs remain unchanged. Focused frontend/backend tests, migration checks, Nuxt build, responsive/catalog guards, flow freshness and two real-browser geometry cases pass. |
| Panel — leading unlabeled kebab action column | 🟡 Ready for merge | 2026-08-27. Documents, Proposals, Diagnostics and classic/grouped Incomes now render Checkbox → Actions → Identity/Content. The accessible but visually empty action track is fixed at 56 px outside proportional data sizing; click/auxclick cannot trigger row navigation and trusted touch pan remains available from the button. `rowActionsLayout` keeps legacy loose-icon tables, including Collection Accounts, explicitly out of scope until product consolidates them. Focused unit coverage and 32 targeted Playwright scenarios are green. |
| Ops — backend environment and Git object maintenance | ✅ Done | 2026-08-27. Thirteen compatible transitive/tooling packages were validated in an isolated candidate and promoted to the production venv; `pip`, `pip check` and `pip-audit` are current/clean, and Gunicorn/Huey restarted healthy. The merged `vuln-audit` worktree was retired without deleting its branch; conservative two-week Git GC reduced loose objects 7,003→0 and packs 34→2, removed stale `gc.log`, preserved recent dangling objects and passed connectivity/fetch checks. Direct manifest modernization is tracked by the 2026-08-28 dependency refresh below. |
| Accounting — permanent client catalog in bulk assignment | 🟡 Ready for merge | Updated 2026-08-28. Bulk client assignment now opens with a permanent in-flow catalog instead of a focus-triggered dropdown, so no blank height is reserved. Five rows fit before the catalog's sole scrollbar; name/company/email and missing-email state identify every client. Search filters in place, later pages preserve order, Nombre toggles A-Z/Z-A and remembers the browser-local choice. Count and every affected record remain visible while the modal stays still; at 412×915 it fills the viewport. The array API remains backward-compatible and adds optional `order=name|-name`. Backend/store/component/modal tests and three registered Playwright scenarios pass; other modal selectors retain the shared floating layer. |
| View Map — contextual Explorer and guided tours | ✅ Done | Updated 2026-08-27. `/panel/views` now explains ProjectApp through three spaces: Panel interno (8 main modules), Plataforma de clientes (8) and Experiencias públicas (4). Every module exposes representative submodules, purpose, operational value, actors, stage, relations and secondary technical references. Hover/focus previews context without changing the URL; free selection uses `node`, and guided tours use `tour=<space-id>` while preserving the current node on exit. Compact/portrait profiles use cards and landscape+ keeps the orbit. The taxonomy validator proves exact ownership of all 104 routes and seven sections. Focused catalog, component and query-state tests, view/static guards, Nuxt production build and all 16 focused Playwright cases pass; the flow registry is fresh and `admin-view-map` has success, failure and display coverage without gaps. |
| Documents — recoverable observation deletion + panel-owned dialogs | 🟡 Ready for merge | 2026-08-26. Observations now distinguish **Discardar** (retains issue + optional reason) from **Eliminar** (soft-deletes any status into a recoverable trash). Single and atomic bulk deletion reconcile only note-origin pending states; restore reopens/reuses a compatible state or rolls back. Actor/time-only events avoid copying content; REST and the 17-tool Documents MCP share the service. The note modal shows full content and external-copy warning. All 14 reachable native panel dialogs were replaced, an orphan removed and CI now blocks `alert/confirm/prompt`. Focused backend/MCP/unit coverage, migration checks, Nuxt build and registered E2E flow accompany the change. |
| Panel — disabled controls explain every blocker | 🟡 Ready for merge | 2026-08-26. `BaseControlGate` exposes every resolvable prerequisite as adjacent text plus hover/focus/touch help; shared button/action/input/segmented primitives accept explicit disabled reasons, and transient work remains visibly labelled. The panel sweep closed all 85 actionable silent controls and a strict CI guard prevents recurrence. The collection-account modal now flags clients without email in the selector, persists a canonical email inline without losing the draft and lists all preview blockers together. Focused Jest 29/29, Playwright 2/2, Nuxt build and the four-outcome P1 flow audit pass. |
| Documents — business-priority column order | ✅ Done | Updated 2026-08-27. The fixed list order is Actions → Title → States → Created/Date/Archived → Client → Project. Landscape retains Actions/Title/States/Date and groups Client/Project under Title; compact cards keep Title/States primary with Date → Client → Project metadata. Actions remains first, title containment and width persistence stay intact, and no user-specific column order, backend or schema change was added. Revisit Project priority after PA-55 backfills links. |
| Accounting — grouped collection accounts by client/project | ✅ Done | 2026-08-26. Cuentas de cobro now switches between classic and grouped views, with one client/project criterion, pending-desc ordering, unassigned-last handling, live vs historical project separation, five amount/count metrics per group, five status counts and a filtered-set footer. The global preference saves immediately and is also editable in Accounting Settings. Backend settings/MCP contracts, focused unit tests, four grouping E2E outcomes, the Settings persistence flow, production build and flow registry are covered. |
| Accounting — ingreso vinculado abre en Esperados | 🟡 Ready for merge | 2026-08-26. El modal de cuenta de cobro parte siempre en ingresos esperados del cliente, filtra por `IncomeRecord.kind` para conservar esperados parcialmente pagados y no recuerda la última selección. Todos/Líquidos siguen disponibles. El vacío ofrece **Ver todos**: primero cambia el tipo manteniendo el cliente y, si éste no tiene ningún ingreso elegible, amplía el alcance. Sin cambios de API ni esquema. |
| Panel — intrinsic-safe text containment | ✅ Done | 2026-08-26. Shared table/list primitives now classify content as wrap/truncate/atomic; arbitrary data strings use bounded `min-w-0` boxes with `overflow-wrap:anywhere`, badges wrap safely, and only bounded dates/money/numbers stay atomic. Representative Task, Project, Client, Accounting, Email, Content and MCP lists adopted the contract. Focused Jest consumers, Nuxt build, the 103-view/13-module/five-viewport responsive contract and static/flow gates pass. |
| Projects — real lifecycle states, contextual help and non-close endings | 🟡 Ready for merge | Updated 2026-08-30. PA-49 consumes PA-88's shared, catalog-scoped state/group/episode/event model. Six seeds cover En desarrollo, Activo, En evolución, Suspendido, Completado and Dado de baja. Suspendido is the only reversible stop; the former Pausado state, compatibility mirror and episodes migrate into it while preserving caused debt and cancelling open future billing. Labels, descriptions and colors are administrable; immutable operational effects drive billing, while derived consequence help prevents editable copy from changing business meaning. En evolución and Activo intentionally share the operating effect. Contextual help appears in counts, filters, rows, cards, catalog and transition modal with hover/focus/touch access. Changes use preview + stale-safe token + atomic consequences and dated history. Completado requires a clean close; Dado de baja cancels future service and forces per-income keep/write-off decisions, with a note on direct jumps. Legacy archived rows remain Sin clasificar/Por revisar and fail closed for money. Filters/counts, fake data, MCP contract and P1 E2E flows are updated. Integration remains PR/CI-controlled. |
| Transversal — representative deterministic fake data | 🟡 Ready for merge | 2026-08-26. One guarded, atomic orchestrator now seeds every visible module with default volume 60, a 60-client/67-project skew, 60-row platform lists, coherent accounting→document origins, 60 communication threads/264 messages, auxiliary Email/QR/Linktree/LinkedIn/MCP history, temporal buckets and intentional long-name/large-amount extremes. Shared seed + anchor replay, no known default passwords, hard-false production settings and a 102-model executable ownership contract close the drift. The focused contract passes 25/25 in policy-sized batches; its gate has no errors and QA classified all 25 tests as KEEP. Integration remains PR/CI-controlled. |
| MCP — Communications + connector parity | ✅ Done | Updated 2026-08-31. The token-authenticated connector now exposes six tools. `update_message` edits one active outgoing draft in place, replaces same-client Document references, applies existing reply/channel rules, returns the complete refreshed message and appends an attributed old/new revision without delivering through either channel. Sent, received, failed, voided and cross-client attempts remain unchanged on rejection. The connector still lists/opens threads, creates client-owned threads/messages and records confirmed sends through the panel services. The nine-connector field contract, focused regression suite, safe inactive/tokenless seed and `docs/MCP_VALIDATION_RUNBOOK.md` keep model/tool drift visible. |
| Security — systematic dependency refresh 2026-08-28 | 🟡 Ready for merge | Backend is current through Django 6.1, Faker 40.37, Gunicorn 26.2, pytest-cov 7.1 and ReportLab 5; production Python 3.12/MySQL 8.4 satisfy the framework floor and email uses the forward-compatible `MAILERS` API. Frontend is current through Nuxt 4.5.2, Vue 3.5.42, Pinia 4.0.3, VueUse 14.4, marked 18, Swiper 14, ApexCharts 7 and Vue Test Utils 2.5; four unused packages and one orphan setup file were removed. `pip-audit` and `npm audit` report zero vulnerabilities. Jest/Babel remain on their newest Vue-Jest-compatible line; `vuedraggable` 4 is retained because npm's nominal latest 2.24.3 is the older Vue 2 line. Every dependency commit passed full PR CI before the next update began. |
| Documents — user-managed multi-state episodes | ✅ Done | 2026-08-25. The visible tag/status overlap was consolidated into one administrable workflow catalog: exclusive cycle + additive signals, editable seeds, inline creation with similarity suggestions, merge/retire safeguards, high-attention needs-fix display, OR/absence/preset filters, exact/relative durations and a full attributed episode timeline. Normalized observations can open/close linked needs-fix episodes; confirmed standalone email delivery can open Enviado; MCP exposes state and note operations. Client visibility is now independent, collection accounts retain their commercial lifecycle, and migration `0210_document_state_episodes` maps Published only to visibility while expanding legacy tags/notes without inventing dates or cycle states. Focused verification: 38 backend cases, 59 frontend unit cases, 19 E2E cases, migration graph/SQL checks, flow registry/audit and Nuxt production build. |
| Panel — catálogo transversal de iconos de acción | ✅ Done | 2026-08-25. Catálogo ejecutable de 84 acciones en Heroicons 24 Outline, adoptado en las 51 páginas `/panel` y componentes compartidos alcanzables, incluido el módulo concurrente de Comunicaciones. `BaseActionIcon`/`BaseActionButton` centralizan símbolo, etiqueta, tooltip/foco, nombre accesible y feedback estable; `BaseButton` conserva targets táctiles de 44 px. `/panel/styleguide` expone el inventario, `docs/panel-action-icon-catalog.md` documenta conflictos/decisiones y el guard de CI protege 273 archivos contra drift. Flow-map fresh; auditoría 261 covered, 39 partial, 0 junk-only, 0 missing, 34 exempt. |
| Client Communications Registry | ✅ Done | 2026-08-25. Decided as a separate product module (implemented inside the existing `content` app), not a Document subtype. `/panel/communications` records multiple client threads, optional project scope, ordered incoming/outgoing email or WhatsApp messages, draft/sent/received facts, derived Respondido state, protected bidirectional Document references, annulments and append-only date corrections. Project client changes detach historical threads; client/document deletes are guarded. Includes migration `0210_communications_registry`, fake data, focused API/store/component/config coverage and registered four-outcome E2E flow. Manual registration is the chosen product workflow; templates, imports or provider integrations would require separately approved requirements. Decision record: `docs/superpowers/specs/2026-08-25-client-communications-registry-design.md`. |
| Documents — contained, readable and resizable title column | 🟡 Ready for merge | Updated 2026-08-28. Long names use one contained ellipsized line in rows/cards and one measured, viewport-aware `BaseTooltip` plus **Ver completo/Contraer** whether they contain spaces or not; complete names add no hint, actions reuse the same primitive and no native `title` competes with it. Measurement repeats after web fonts load, expansion wraps anywhere and supplies the touch path. Folder is first on a separate metadata row, followed by compact Client/Project/State distinctions; desktop rows without a folder keep natural height. The visible labelled Título separator resizes 240–520 px, where 520 covers the current 40-title/56-character production boundary (496 px estimated with padding/safety), persists under `projectapp-table-widths:documents-list`, resets to 320 px, and preserves Proyecto→Cliente→Fecha donors with fixed Estados/Acciones. No backend/schema change. |
| Documents — contextual return from editor | ✅ Done | 2026-08-25. The Documents list now encodes folder, scope, states, client/project, global search, ordering, view, page and focus in its URL. Every editor exit validates and returns to that origin with a contextual label; direct/untrusted entries fall back to the localized root, while browser Back restores the original route naturally. The explicit link restores and focuses the owning row/card. Coverage: 32 focused unit tests, 3 Playwright scenarios without retries and a successful Nuxt build. The cross-module follow-up inventory is in `docs/audits/2026-08-25-list-detail-return-navigation.md`. |
| Emails — configurable BCC copies of every outbound send | ✅ Done | 2026-08-26, completed 2026-08-29. `EmailDeliveryGateway` is the sole SMTP boundary and rejects any key outside the exact 56-channel inventory. Eight configurable families cover client, supplier, agency, accounting, operations and security/OTP traffic; every successful primary is followed by independent BCC attempts, deduplicated against the original envelope, and copy failures never block or retry the primary. Universal `EmailLog` history nests sent/failed/skipped copies, supports family/recipient/status/date filters and lets any admin inspect the retained full body, including security emails by explicit policy. Inventory: `docs/client-email-copy-inventory.md`; migrations `0213` and `0225`. The latter resolves the empty production catalog by provisioning `carlos18bp@gmail.com` active for all eight families while preserving panel administration. |
| Accounting — Recurrentes: recálculo mensual COP | ✅ Done | 2026-08-22. `cop_equivalent` pasó a ser un cache derivado y de solo lectura: precio/moneda usan la tasa USD vigente de Configuración y periodicidad prorratea el costo mensual. Editar cualquiera de las tres entradas refresca fila, subtotal por categoría y total general; cambiar la tasa resincroniza todos los USD. La migración `0208` corrige filas históricas y el formulario muestra una previsualización no editable. |
| Panel — Responsive Standard Phase 0 | ✅ Done | 2026-08-22. `docs/RESPONSIVE_STANDARD.md` define cinco viewports de aceptación (412/835/1195/1440/2560), máximos de 1400/1600 px, patrones canónicos por tipo y el censo de 47 superficies renderizables. Inventario: 16 hallazgos (1 P0, 11 P1, 4 P2); PA-45/61 quedan en sus olas, PA-66/73 se consolidan en el criterio compartido. Fase documental: sin cambios de producto. |
| Panel — Responsive Final Audit Phase 5 | 🟡 Audited | 2026-08-22. Las cinco fases fueron auditadas sobre `main` con clasificación triestado. Se corrigieron Proyectos (fila/tarjeta + segmento/select), el breakpoint local de Blog edit y las acciones hover-only de Kanban/Perfil; QA independiente aprobó contrato 101/12/5, unit 7/7, matrices focales 20/20 (Proyectos 5 + Perfil/Kanban 15) y flujos de selector/guardado 2/2. El informe final registra RSP-01…16, PA-45/61/66/73, correcciones y RSP-F5-01…04. Permanece amarillo hasta certificar hardware físico y resolver/versionar overlays y harness/ledger residuales. |
| Responsive Standard — PA-75 through Phase 4 | ✅ Done | 2026-08-22. The 101 Nuxt pages are owned by 12 responsive modules over the canonical 412/835/1195/1440/2560 matrix. Shared table/list, filter, modal, navigation, action and page-shell contracts were adopted through Commercial, Emails, Document Canvas, Dashboard, Content, MCP and public views; earlier PA-45/61/66/69/70/73 instances are governed by the same standard. Playwright supplies a written five-width script per module, PR CI runs affected modules, monthly CI runs all modules, and a February/August issue reviews device assumptions. No backend or schema changes. |
| Business Proposal — Core Models | ✅ Done | BusinessProposal, ProposalSection, ProposalAlert, RequirementGroup/Item |
| Business Proposal — Public View | ✅ Done | Fullscreen horizontal scroll, 12 section components, GSAP animations |
| Business Proposal — Refined Closing & Technical Traceability | ✅ Done | 2026-08-21. `FinalNote` usa dos columnas desde `xl`, con ancho útil medido de más de 520 px por tarjeta a 1366 px; los prerrequisitos viven en un disclosure cerrado y CTA/contactos en `ProposalClosing`. Los pagos reservan una línea indivisible para monto, moneda e IVA. Los 14 campos introductorios tienen un contrato de uno o dos énfasis `<b>` seguros en ambos prompts; `0206` normaliza solo `ramon-emiliani` por coincidencia exacta. `ProposalSummary` no crea analítica implícita. Los items incluidos requieren al menos un `linked_item_ids` antes de guardar el detalle técnico; los opcionales no seleccionados solo generan advertencia. Los prompts mantienen levantamiento, QA/despliegue separados, garantía contractual, historias de 1–3 puntos y modelos resilientes de fuentes/caché. |
| Propuesta comercial — Alcance público cerrado de Aerocivil/Ramon | ✅ Done | 2026-08-21. Propuesta 115 actualizada en el mismo borrador: $280M COP + IVA, pagos 40/30/30 de $112M/$84M/$84M + IVA, 8 semanas aceleradas, garantía contractual de un año, hosting en cero, staging/producción del cliente y sin módulos opcionales, PWA, cuentas, roles, administración, analítica ni reportes. El alcance consta de 8 cupos fuente-producto delimitados, 37 items incluidos y 85 requerimientos técnicos con trazabilidad comercial-técnica completa. El total y las cuotas conservan el sufijo fiscal en inversión, resumen y cierre. Se verificaron ORM/API, los PDF comercial y técnico y la ruta pública; las automatizaciones permanecen pausadas. |
| Business Proposal — Contract and Terms Mode | ✅ Done | 2026-08-21. Fourth public mode, **Contrato y condiciones**, for eligible Spanish proposals. It lazily renders the current masked global contract as a full-width intro/index + one continuous clause-linked document contained in an accessible paper surface with semantic border, layered shadow and a decorative back sheet. The unsigned `BORRADOR` PDF is downloaded only from the persistent floating action; the introduction has no duplicate CTA. `show_contract_terms` remains independent from prompt and section JSON. Analytics persist `view_mode=legal` + clause keys; three registered E2E flows cover public display/navigation/gates/retry/download and admin success/rollback. |
| Business Proposal — Admin CRUD | ✅ Done | Create, edit, send, duplicate, JSON import, section editor, defaults, alerts |
| Business Proposal — Local proposal-create skill | ✅ Done | 2026-08-20. Codex + Claude workflow for new proposals: adaptive questionnaire, active 18-section template, commercial JSON + decision manifest, ROI evidence, technical traceability audit, dry-run and explicitly confirmed draft creation through the canonical serializer/service. Drafts remain unsent with automations paused; duplicate and module-default guards are mechanical. |
| Business Proposal — Email System | ✅ Done | 73 content email templates (37 HTML + 36 TXT), automated reminders, cooldown, pause, admin notifications |
| Business Proposal — Initial Email Refresh + PDF Attached | ✅ Done | New `email_intro` TextField on `BusinessProposal` (migration `0117`); `proposal_sent_client` HTML/TXT rewritten with phase card, executive summary, payment breakdown iterating `paymentOptions`, signature; `_build_initial_email_context` reads `INVESTMENT.paymentOptions` + `TIMELINE.totalDuration` from sections via `ProposalSection.SectionType` enum; new shared `_attach_commercial_pdf` helper auto-attaches the commercial PDF (reused by `send_acceptance_confirmation`, fixing prior `Propuesta_comercial_` casing inconsistency); registry entry updated with `closing` editable field + new variables; admin form gets `email_intro` textarea (`data-testid=edit-email-intro`); 5 new tests. |
| Business Proposal — Multi-Proposal Send (Single Email) | ✅ Done | New `POST /api/proposals/<id>/send-multi/` view + `?client_id=` filter on `list_proposals`. `ProposalEmailService.send_multi_proposal_to_client(proposals)` builds one email with N PDF attachments + N `EmailLog` rows sharing a `group_uuid`. `ProposalService.send_multi_proposals(proposals)` applies per-proposal side effects (draft→sent + reminders, expired→reopen + extend `expires_at`, sent/viewed/negotiating→reset timers) using the `BusinessProposal.Status` enum; validates same-client. New `proposal_multi_sent_client` template (HTML/TXT) iterates phases ("Propuesta N de M"). Modal `ProposalMultiSendModal.vue` (BaseModal) lists same-client proposals grouped by status (Borradores / Enviadas-Vistas-Negociación / Expiradas), pre-selects current, max 10, error surface, reusable `frontend/utils/formatMoney.js`. New action item in `ProposalActionsModal`. 6 new pytest cases; `admin-proposal-multi-send` flow registered (P1, ❌ Missing E2E spec). |
| Business Proposal — Analytics | ✅ Done | View tracking, section time, heat score, session tracking, engagement signals |
| Business Proposal — PDF | ✅ Done | ReportLab generation, downloadable from proposal page |
| Business Proposal — Share Links | ✅ Done | UUID share links with independent tracking |
| Business Proposal — Investment Calculator | ✅ Done | Interactive modal for payment options |
| Hosting — 9/6/3-month commercial periodicities | ✅ Done | 2026-08-20: annual offering replaced by `nine_month` across proposal admin/defaults, public view, PDF, platform subscriptions, accounting hostings/cycles, MCP, seeds and flow registry. Current records migrate safely; closed proposals, inactive hostings and paid history retain legacy labels/values. |
| Business Proposal — Client Responses | ✅ Done | Accept, reject (with reason), negotiate |
| Business Proposal — Expiration | ✅ Done | Auto-expire via daily Huey cron task |
| Business Proposal — Change Log | ✅ Done | Full audit trail (20+ change types) |
| Business Proposal — Email Deliverability | ✅ Done | Dashboard with send/delivery/bounce rates |
| Business Proposal — Email Templates Editor | ✅ Done | View, edit, preview, reset email content |
| Business Proposal — Default Config | ✅ Done | Per-language default section templates |
| Business Proposal — Clients List | ✅ Done | Real `accounts.UserProfile` client entities power `/panel/clients/`, proposal autocomplete, orphan cleanup, and snapshot sync back to linked proposals |
| Responsive Plan — Phase 3: Documents, Clients & Projects | ✅ Done | 2026-08-22. The three panel modules now consume the canonical responsive config and its five-viewport contract (412/835/1195/1440/2560): compact drawers and cards below `PANEL_BREAKPOINTS.landscape`, two-zone/table layouts at the landscape device, phone full-screen modals, touch-only alternatives for row actions/reassignment, and a 1400 px content cap. One Playwright matrix navigates through the panel and validates all 15 module/viewport combinations without page overflow. |
| Portfolio Works — Public | ✅ Done | Listing and detail with bilingual structured JSON |
| Portfolio Works — Admin CRUD | ✅ Done | Create, edit, delete, duplicate, cover image upload, JSON import |
| Blog — Public | ✅ Done | Listing with featured hero, categories, pagination, detail with JSON/HTML |
| Blog — Admin CRUD | ✅ Done | Create, edit, delete, duplicate, cover image upload, calendar view, JSON import |
| Blog — Sitemap | ✅ Done | Sitemap data endpoint + XML rendering |
| Portfolio — Sitemap | ✅ Done | Sitemap data endpoint |
| Contact Form | ✅ Done | Public submission with budget ranges, email notification |
| Landing Pages (3) | ✅ Done | Web design, software, apps — with animations |
| Home Page | ✅ Done | Marketing page with GSAP animations |
| About Us Page | ✅ Done | Company information page |
| Internationalization (i18n) | ✅ Done | EN/ES with prefix routing, lazy loading, geo-detection |
| Admin Auth Middleware | ✅ Done | Session/CSRF check, redirect to Django admin login |
| CI/CD Pipeline | ✅ Done | GitHub Actions: pytest, Jest, Playwright (5 shards), quality gate |
| Panel responsive plan — phases 0 and 1 | ✅ Done | 2026-08-22. Canonical bands/reference devices and a prioritized module inventory are documented in `docs/RESPONSIVE_STANDARD.md`. `frontend/config/responsive.js` drives namespaced Tailwind screens and Playwright viewports. Shared primitives cover page width, priority-declared tables, tabs/filters, modal kinds/form stacking, navigation/drawer accessibility, row/bulk actions, typography and touch discovery. Accounting/table/filter/tab compatibility aliases preserve current callers; `/panel/styleguide` is the executable example and `admin-styleguide` validates all five widths. Later module phases adopt these primitives rather than creating new responsive rules. |
| Panel responsive plan — phase 2 accounting | ✅ Done | 2026-08-22. The twelve accounting tabs adopt the shared shell, compact navigation/filter selectors, business-ranked KPI groups, explicit per-column policies, grouped compact headers, semantic mobile modals and touch-safe action menus. Pocket relocates running balance instead of hiding it. Automated acceptance covers all five reference widths and `docs/ACCOUNTING_RESPONSIVE_TEST_SCRIPT.md` records the repeatable 12×5 walkthrough plus long-modal scenarios. |
| Security & Build Warning Hardening | ✅ Done | 2026-08-19: `npm audit` y `pip-audit` en cero; cinco constraints opcionales compatibles con MySQL mediante `NULLIF`; autoimports de status sin colisiones; ApexCharts lazy/client-only y ningún chunk cliente supera 500 KB. El build estático mantiene payloads inline y limpia `staticfiles` antes de publicar para impedir payloads HTML, colisiones archivo/directorio y chunks huérfanos. |
| Codex Ecosystem Methodology | ✅ Done | Codex-first runtime documented and implemented: `AGENTS.md` hierarchy, native repo skills in `.agents/skills/`, project config in `.codex/config.toml`, canonical guide + quickstart, legacy compatibility notes |
| MCP Concurrent Startup Capacity | ✅ Done | DRF throttle buckets registered MCP connectors by client IP + connector slug, while every unknown slug shares one defensive bucket. Five legitimate connectors can initialize concurrently without consuming one another's quota. |
| Deployment (Production) | ✅ Done | Gunicorn + Nginx + systemd, documented in deployment-guide.md |
| Content migrations — merge parallel `0204` leaves | ✅ Done | `0205_merge_contract_terms_and_client_communication` reunifies the contract-terms and private document-communication branches without schema or data operations. |
| WhatsApp Notifications | ✅ Done | CallMeBot API integration |
| Database Backups | ✅ Done | django-dbbackup with rotation |
| Query Profiling (Silk) | ✅ Done | Optional, env-gated |
| Platform — Auth & Onboarding | ✅ Done | JWT login, OTP verify, complete-profile, middleware gating |
| Platform — Dashboard | ✅ Done | Admin KPI cards + recent clients; Client profile summary |
| Platform — Projects & Kanban | ✅ Done | Project CRUD, detail hub, 3-column kanban board, drag & drop, comments |
| Panel — Projects Module (Plataforma space) + Fly-Create | ✅ Done | PA-49: `/panel/projects` is the commercial face of `accounts.Project`, with session-side admin endpoints, literal clients-without-projects indicator, non-blocking duplicate warning, superuser links into project-filtered accounting, immutable-client cascade and fly-create selectors. The original archive/unarchive lifecycle has been superseded by the shared real-state flow above; those legacy endpoints now return 410. |
| Platform — Client Management | ✅ Done | Admin invite, list, detail, edit, deactivate, reactivate |
| Platform — Sidebar & Layout | ✅ Done | Collapsible sidebar, mobile drawer, theme toggle, role-based nav |
| Platform — E2E Coverage | ✅ Done | Platform flows are registered and covered across auth, dashboard, projects, kanban, clients, collection accounts, data model, notifications, payments, and related routes |
| Document System — Model + Admin CRUD | ✅ Done | `Document` model (uuid, title, slug, status, language, cover_type); panel pages (index, create, edit); `documents.js` store |
| Documents — Private notes and client communication | ✅ Done | 2026-08-25. The create/edit modal is “Notas”: it retains the optional email subject, email body and WhatsApp fields, and supports ordered custom title/content notes. On edit, **Guardar cambios** persists the notes immediately with visible confirmation and leaves unrelated editor changes pending; on create, **Aplicar al borrador** explicitly says creation is still required. Every value remains individually copyable, issued collection accounts remain read-only, and PDF, list API and client portal never expose the private metadata. |
| Document System — Project folders separated from personal folders | 🟡 Ready for merge | Updated 2026-08-29. Every project has one protected root with Cuentas de cobro, Propuestas, Entregables and QA; the two generated categories share stable keys with the filing service, so project accounts and proposal snapshots live under this root instead of a parallel tree. The sidebar diagnoses `no_projects`, pending reconciliation, an empty state filter and readiness failure while preserving partial results. Manifest v2 adds reviewable exact filing actions for eligible folderless project accounts, flags manual locations as conflicts and records inverse locations; neither live filing nor the general backfill can create an unreconciled historical root. No production conversion is automatic. Focused backend, unit and 3-outcome E2E slices plus the 356-flow audit are green with 0 missing/junk-only. |
| Admin Panel — Internal Kanban Task Board | ✅ Done | `Task` model (status/priority TextChoices, position, assignee FK SET_NULL, due_date); migration `0087_task.py`; 5 FBV endpoints (`tasks/`, `tasks/create/`, `tasks/<id>/update/`, `tasks/<id>/reorder/`, `tasks/<id>/delete/`); `TaskListSerializer` (assignee_name, is_overdue via context today) + `TaskCreateUpdateSerializer`; `tasks.js` Pinia Options API store (fetchTasks, createTask, updateTask, moveTask, deleteTask, replaceTaskInPlace); `TaskCard.vue` (priority badge, due_date red if overdue), `TaskColumn.vue` (vuedraggable cross-column DnD), `TaskFormModal.vue`; `/panel/tareas` page with `useConfirmModal`; panel nav entry "Tareas" / "Kanban" as first section; 11 backend tests + 9 frontend unit tests + 2 E2E specs; `ADMIN_KANBAN_TASKS` flow tag registered |
| Business Proposal — Admin UX Improvements | ✅ Done | Inline "days" number input beside `datetime-local` on create + edit pages (bidirectional sync, time-preserving); `@temp.example.com` domain bypass in `validate_email_domain_mx` + `jsonForm` default; fixed bottom-right save toast with Tailwind `<Transition>`, `clearTimeout` guard, 5 s auto-dismiss |
| Business Proposal — Value Added Modules Section | ✅ Done | New `section_type = 'value_added_modules'` (migration `0102`). `ValueAddedModules.vue` public component renders card grid of included-free modules (icon, title, justification, description). Seeded from `functional_requirements.manual_module` group for existing proposals. `test_value_added_modules.py` covers default schema; `ValueAddedModules.test.js` created (not yet run locally). |
| Business Proposal — ROI Projection Section | ✅ Done | New `section_type = 'roi_projection'` (migration `0118`) sitting at `order=4` between `conversion_strategy` and `investment` so the sponsor sees quantified outcomes (visualizations/day, sponsor reach, MRR, payback, year-1 revenue, scenarios cons/realistic/optimistic) **before** the price ask. `RoiProjection.vue` public component (cards grid + scenarios block + CTA banner). Admin form added to `SectionEditor.vue` with drag-drop KPIs and scenarios + 3 cases in `sectionEditorUtils.js`. **Web-only**: intentionally absent from `SECTION_RENDERERS` so it skips the PDF; PDF loop now also drops the TOC entry for sections without a renderer (consistent with `proposal_summary` and `process_methodology`). `linkify` hardened to escape HTML by default (whitelist of `<b>/<strong>/<i>/<em>/<br>`) before injecting into `v-html`. Migration backfilled an empty `is_enabled=False` row in every existing proposal + bumped existing orders >=4 by +1. **Caveat**: backfill silently no-op'd because `ProposalDefaultConfig (es)` had a stale 16-section list — fixed via shell using the canonical `DEFAULT_SECTIONS` import. Pre-existing order collision `final_note=14, next_steps=14, technical_document=15` resolved as 14/15/16 in source + prod. Albunmanía proposal (id=91) populated with the financial copy ($122M investment, payback 5–9 mo, 6 KPIs, 3 scenarios). Tests: 11 backend pytest + 14 frontend unit (8 sectionEditorUtils + 6 linkify) + 2 E2E specs. `EXPECTED_DEFAULT_SECTION_COUNT` 16 → 17. Flow `proposal-roi-projection` (P1) registered. |
| Diagnostic Public View — PDF Download, Share & Dark Mode | ✅ Done | `DownloadDiagnosticPdfButton.vue` (FAB, calls `GET /api/diagnostics/public/{uuid}/pdf/`); `ShareDiagnosticButton.vue` (FAB, copy-link + Web Share API, bilingual, slide-up modal); `useDiagnosticDarkMode.js` (module-level singleton, persists to localStorage, applies `data-theme` to `[data-diagnostic-wrapper]`); `diagnostic/[uuid]/index.vue` redesigned with esmerald brand tokens, `rounded-3xl` cards, theme toggle; public section components updated with brand tokens; new `DiagnosticPdfService` backend service mirroring `ProposalPdfService`; `download_public_diagnostic_pdf` view (`GET /api/diagnostics/public/<uuid>/pdf/`, AllowAny, gated by `PUBLIC_VISIBLE_STATUSES`). |
| MarkdownAttachmentModal — Diagnostic Template Copy | ✅ Done | `showDiagnosticTemplates` Boolean prop on `MarkdownAttachmentModal.vue`; 3 copy buttons (Aplicación → Técnico → Anexo) above markdown textarea; lazy fetch via `GET /api/diagnostic-templates/<slug>/`, per-slug `reactive({})` cache; `copyFeedbackTimer` tracked + cleared on close/unmount; `DiagnosticEmailsTab.vue` passes `show-diagnostic-templates`. 3 new unit tests pass. |
| Business Proposal — Value Added Modules Order Fix | ✅ Done | `proposal_service.py` seed reordered: `functional_requirements` → order 9, `value_added_modules` → order 10. Migration `0103_swap_value_added_modules_order.py`. Public view hides `value_added_modules` when no `module_ids` resolve against FR groups. `useSellerPrompt.js` updated with correct index and narrative position. |
| Business Proposal — Calculator Module Reorder + Seller Prompt Auto-Select | ✅ Done | `additionalModules` array reordered in ES + EN to business-priority order (payments → AI → marketing → UX). `reports_alerts_module` renamed to include WhatsApp; 6th item (💚) added to `items` list. `_seller_prompt` in `proposal.py` gained `CRITICAL_additionalModules_autoselect` key with keyword-to-module detection mapping (DIAN → `integration_electronic_invoicing`, PSE/Wompi → `integration_regional_payments`, etc.) so Claude auto-sets `default_selected: true` + adapts copy when requirements mention a module. `CRITICAL_functionalRequirements` count made count-agnostic. Tests in `test_proposal_service.py` updated (new order + 6-item assertion). |
| Business Proposal — Personal Slug URL | ✅ Done | `slug = SlugField(unique=True, blank=True, max_length=120)` on `BusinessProposal`. Auto-generated from `ProposalDefaultConfig.default_slug_pattern` (e.g. `{client_name}`) via `render_slug_pattern`; collision-free via `resolve_unique_slug` (1 DB query via `__startswith` + Python set). Migrations `0104` (populate+dedupe+`unique=True`) + `0105` (`default_slug_pattern` field on `ProposalDefaultConfig`). New URL `proposals/by-slug/<slug>/` delegates to `_serve_public_proposal` helper; UUID route fully preserved for backward compatibility. `'slug'` added to `tracked_fields` for audit log. Frontend: `frontend/utils/slugify.js` (`toSlug()` NFD normalize); `fetchPublicProposal` UUID/slug branch detection; editable slug input on proposal edit General tab (Save + Regenerate); live pattern preview in `ProposalDefaultsPanel.vue`. `public_url` property uses `slug or uuid`. Tests: `TestBusinessProposalSlug` (auto-gen, collision suffix, preserved) + `TestBusinessProposalPublicUrl` updated + 4 new by-slug API tests. |
| Documents Tab Reorganization & doc_refs Email Attachment | ✅ Done | Documents tabs are now pure document management (list/download/upload). "Enviar al cliente" sections removed from both Proposal and Diagnostic Documents tabs. Diagnostic MD templates (Aplicación/Técnico/Anexo) moved from Proposal aside → Diagnostic "Documentos" tab. New `AttachFromDocumentsModal.vue` (shared) + `useDocRefsAttachment.js` composable added. Both Emails tabs gained "Adjuntar desde Documentos" button that passes `doc_refs` JSON field to the send endpoints. Backend: new `_doc_refs.py` shared helper (`parse_doc_refs_field` + `DocRefError`), `_resolve_proposal_doc_refs` and `_resolve_diagnostic_doc_refs` resolvers with pre-batched ORM queries. `TEMPLATES_DIR` made public in `diagnostic_template.py`. Deleted: `SendDocumentsModal.vue`, `ProposalDiagnosticTemplatesSection.vue`, `SendDiagnosticDocumentsModal.vue` + their tests. Orphaned store actions `sendProposalDocuments`/`sendDiagnosticAttachments` + backend endpoints still exist — cleanup pending. |
| Diagnostic Templates Exposure in Proposal Tab | ✅ Superseded | Superseded by "Documents Tab Reorganization" (Apr 22). `ProposalDiagnosticTemplatesSection.vue` deleted; MD templates are now listed in `DiagnosticDocumentsTab.vue` (the correct owner). `diagnostic-templates/` API endpoints remain in use. |
| Admin Panel — Unified Defaults Page | ✅ Done | `frontend/pages/panel/defaults.vue` shell with mode switch (Propuesta/Diagnóstico) synced to `?mode=` query param via `computed`. Two panel components extracted: `ProposalDefaultsPanel.vue` (6 tabs) + `DiagnosticDefaultsPanel.vue` (5 tabs) under `components/panel/defaults/`. Both loaded via `defineAsyncComponent`. Old routes (`/panel/proposals/defaults`, `/panel/diagnostics/defaults`) become redirect stubs preserving `?tab=`. Sidebar renamed to "Defaults" and moved to top of Sales. All internal links updated. No backend changes. |
| Diagnostic Analytics — Full Parity with Proposal Analytics | ✅ Done | `diagnostic_analytics` view fully rewritten: engagement score (0–100), funnel with drop-off %, device breakdown (tablet/mobile/desktop via UA), sessions history (50 rows, no view_mode column), activity timeline from `DiagnosticChangeLog`, skipped sections, global comparison, 3 N+1 patterns eliminated, legacy compat keys removed, `WebAppDiagnostic.Status` enum replaces stringly-typed checks. New `export_diagnostic_analytics_csv` view + `GET /api/diagnostics/<id>/analytics/csv/` route. `DiagnosticAnalytics.vue` rewritten to ~963 LOC with 12 collapsible sections mirroring proposal tab (minus view_mode comparison, share links, technical engagement). New `test_diagnostic_analytics.py` (10 tests). `/simplify` pass verified no remaining N+1 or quality issues. |
| Diagnostic Markdown-to-PDF Email Attachment | ✅ Done | When diagnostic is in `negotiating` status, "Crear documento desde markdown" button appears in `DiagnosticEmailsTab.vue`. `MarkdownAttachmentModal.vue` (refactored to `endpoint: String` prop — moved to `components/` root for cross-context reuse) generates a branded PDF via `POST /api/diagnostics/<id>/email/markdown-attachment/` → `DocumentPdfService.generate_from_markdown()` → returns inline PDF; seller previews in iframe, then attaches as a `File` object to the email composer. Shared backend helper `content/views/_email_attachment.py` (functions `render_markdown_pdf_response` + `inline_pdf_response`) removes duplication between proposal and diagnostic attachment views. New frontend utilities: `useMarkdownAttachmentHandler.js` composable + `emailAttachments.js` validator + `proposals_constants.js` frozen enum. |
| Web App Diagnostics — Default Config | ✅ Done | `DiagnosticDefaultConfig` singleton-per-language model (`backend/content/models/diagnostic_default_config.py`, migration `0101`) holding `sections_json`, `payment_initial_pct=60`/`payment_final_pct=40` (with `clean()` sum-to-100 guard), `default_currency` (COP/USD), `default_investment_amount`, `default_duration_label`, `expiration_days=21`/`reminder_days=7`/`urgency_reminder_days=14`. `DiagnosticDefaultConfigSerializer` validates `sections_json` shape (`{section_type, title, order, content_json}`) and re-checks the payment sum. `diagnostic_service.py` exposes `DEFAULT_PAYMENT_INITIAL_PCT=60` etc. + `get_default_config(lang)` / `get_default_section_specs(lang)`; `create_diagnostic` reads the config once and forwards it to `seed_sections(diagnostic, config=...)` — no duplicate query. 2 admin endpoints: `GET/PUT /api/diagnostics/defaults/?lang=` and `POST /api/diagnostics/defaults/reset/`. Frontend page `panel/diagnostics/defaults.vue` (5 tabs: General + Sections + Emails-link + Prompt-placeholder + JSON) reuses `usePanelToast`, `useConfirmModal`, `ResponsiveTabs`, `<PanelToast />`. Pinia actions `fetchDiagnosticDefaults`/`saveDiagnosticDefaults`/`resetDiagnosticDefaults` (whitelisted general fields). Header link added to `panel/diagnostics/index.vue`. Tests: 8 model + 16 view backend (24 green) + 6 frontend store unit (jest run pending local install). Side effect: a misplaced `penal_clause_value` field was moved back to `ConfidentialityParamsSerializer`. |
| Web App Diagnostics — Full Module | ✅ Done | **JSON-section architecture** (rewrite Apr 16, 2026): `WebAppDiagnostic` model + new `DiagnosticSection` (8 section_types: `purpose`/`radiography`/`categories`/`delivery_structure`/`executive_summary`/`cost`/`timeline`/`scope`, `content_json`, `visibility` ∈ initial/final/both), `DiagnosticChangeLog`, `DiagnosticViewEvent`, `DiagnosticSectionView`. Migrations `0094` (schema) + `0095` (seed existing rows) + `0096` (drop legacy `DiagnosticDocument`). `content/seeds/diagnostic_template.py` → `default_sections()` seeds the 8-section + 14-category payload via `bulk_create`. Service (`diagnostic_service.py`): `create_diagnostic`, `seed_sections`, `reset_section`, `transition_status` (writes `STATUS_CHANGE` log), `visible_sections(phase)`, `register_view`, `log_change`, `build_render_context`. Email service (`diagnostic_email_service.py`): `send_initial_to_client`, `send_final_to_client`, `send_custom_email` + rate-limited composer. Serializers: `DiagnosticSectionSerializer` + `DiagnosticChangeLogSerializer`; `DiagnosticDetailSerializer` memoizes `build_render_context` per instance and slices prefetched `change_logs` in Python; `PublicDiagnosticSerializer` whitelists `render_context` via `PUBLIC_RENDER_CONTEXT_KEYS`. Views: 18 FBVs under `/api/diagnostics/` — CRUD, sections CRUD (single + bulk + reset), activity log list/create, analytics aggregate, send-initial/mark-in-analysis/send-final, attachments CRUD + send, email composer (send/defaults/history/attach-confidentiality), public retrieve/track/track-section/respond. `_ensure_view_event` dedups session rows; `_send_and_transition` returns `(ok, email_ok)` so the UI surfaces email failures. `get_client_ip` hoisted to `content/utils.py`. Admin page `panel/diagnostics/[id]/edit.vue` has 5 base + 2 status-gated tabs: `general` → `[emails]` (≥SENT) → `[documents]` (≥NEGOTIATING) → `sections` → `prompt` → `json` → `activity` → `analytics`. **UI refinements (Apr 20)**: sticky title format changed to `$X.XXX.XXX COP` via `formatInvestment` (parity with business proposal); `size_category` removed from General tab form; Sections/Activity/Analytics tabs expanded to full page width (no `max-w-4xl`); NDA checkbox added to Correos tab (`attachConfidentiality` flag → backend `_parse_diagnostic_email` generates and attaches the confidentiality PDF or returns 400). The previous consolidated `technical` tab (Pricing + Radiografía sub-tabs) was removed on 2026-04-18; pricing fields live in General, radiography is edited as a regular section, and legacy deep-links (`?tab=pricing|radiography|technical`) redirect to `general`/`sections`. Admin components (`components/WebAppDiagnostic/admin/`): `DiagnosticSectionEditor.vue` + 8 per-type forms under `admin/sections/`, `DiagnosticPromptPanel.vue` + `PromptEditor.vue`, `DiagnosticActivityTab.vue`, `DiagnosticAnalytics.vue`. Shared `usePromptState({storageKey,defaultPrompt})` factory (`composables/usePromptState.js`) + `useDiagnosticCommercialPrompt`/`useDiagnosticTechnicalPrompt`. Public components: 8 section components + `SectionHeader.vue` + `DiagnosticIndex.vue` (sidebar index — hamburger toggle, backdrop blur, floating panel, numbered badges, visited checkmarks) under `components/WebAppDiagnostic/public/`. Public page (`pages/diagnostic/[uuid]/index.vue`): `layout: false`; no default Nuxt header; sections rendered via `COMPONENTS` map; `DiagnosticIndex` sidebar replaces the former pill nav; `visitedIds` ref + `markSectionVisited` helper track visited sections; per-section dwell time tracked with `session_id`, final row flushed via `navigator.sendBeacon` on unmount. `diagnostics_constants.js` exports `DIAGNOSTIC_STATUS`/`STATUS_META`/`SECTION_VISIBILITY`/`VISIBILITY_OPTIONS`/`SEVERITY_LEVELS`/`severityLevelClass`/`ACTIVITY_CHANGE_TYPES`; `arrToText`/`textToArr` re-imported from `BusinessProposal/admin/sectionEditorUtils.js`. Pinia store actions: `updateSection`/`bulkUpdateSections`/`resetSection`/`fetchActivity`/`logActivity`/`fetchAnalytics`/`trackSectionView`. Legacy `.md` templates retained under `templates/diagnostics/_legacy/` (unused at runtime). Tests: 31 backend (18 in `test_web_app_diagnostic.py` + 13 in `test_diagnostic_attachments_and_emails.py`) + 41 frontend (31 store + 10 section-utils roundtrip) — all green. |
| Documents — Las casillas de portada deciden el PDF | ✅ Done | 2026-08-18. La descarga se arma con lo GUARDADO: el botón quedó gateado por el guard de cambios sin guardar (`guardedExport` en `useUnsavedGuard`: «Guardar y descargar» / «Descargar lo guardado» / «Seguir editando»), una línea viva bajo las casillas nombra las páginas (`documentCoverPages.js`, `doc-included-pages`) y `DocumentPdfPreviewModal.vue` muestra el PDF real vía `documents/<id>/pdf/?inline=1`. Vecinos: el correo adjunta con `resolve_blocks` (ya no salta documentos sin parsear), duplicar conserva `template_style`, y las tools MCP aceptan las tres casillas. Guardado, generación y caché quedaron descartados con evidencia (37 documentos con `include_subportada=False` en producción; el generador lee el modelo vivo; no hay PDF persistido). |
| Document System — PDF Generation | ✅ Done | `document_pdf_service.py`, `markdown_parser.py`, `pdf_utils.py` (shared utilities). Branded markdown→PDF is live and reused across proposal/diagnostic attachments and the `/platform/documents` client signing portal. |
| Platform — Quick Access | ✅ Done | `/platform/access` (admin-only); `production_url`, `staging_url`, `admin_url`, `repository_url`, `admin_username`, `admin_password_encrypted` on `Project`; `GET /api/accounts/projects/access/`; Fernet encryption via `accounts/services/credential_cipher.py`; `accounts/admin.py` for data entry; `cryptography>=42,<46` added to requirements; `PROJECT_ACCESS_CIPHER_KEY` env var |
| Panel — Admins Management | ✅ Done | `panel/admins/index.vue` + `panel_admins.js` store |
| Panel — Dedicated Login | ✅ Done | `panel/login.vue` page |
| Platform — Bug Reports | ✅ Done | `platform-bug-reports.js`; global `/platform/bugs` + per-project `/platform/projects/[id]/bugs`; `test_bug_reports.py` |
| Platform — Change Requests | ✅ Done | `platform-change-requests.js`; global `/platform/changes` + per-project `/platform/projects/[id]/changes`; `test_change_requests.py` |
| Platform — Deliverables | ✅ Done | `platform-deliverables.js`; global `/platform/deliverables` + per-project `/platform/projects/[id]/deliverables`; `test_deliverables.py` |
| Platform — Notifications | ✅ Done | `platform-notifications.js`; `/platform/notifications`; `test_notifications.py` |
| Platform — Payments | ✅ Done | `platform-payments.js`; global `/platform/payments` + per-project `/platform/projects/[id]/payments`; `test_payments.py` |
| Platform — Hosting multi-fase con prorrateo | ✅ Done | El hosting de un proyecto factura la **suma de todas sus fases iniciadas** a una frecuencia única (antes: una sola propuesta). Migración `0032` añade `ProjectPhase.hosting_activated_at`. Servicio nuevo `accounts/services/hosting_billing.py` (`phase_billing_amount`, `project_billing_amount`, `prorated_amount`, `phase_tiers`). `_create_subscription_multi_phase` reemplaza a `_create_subscription_from_proposal`: la activación cobra solo fases ya iniciadas (`hosting_start_date` nulo o ≤ hoy); las fases con fecha futura entran después prorrateadas. Tarea Huey `_onboard_due_phases` (dentro de `auto_charge_due_subscriptions`): cuando la fecha de una fase llega, calcula el prorrateo **a precio full sin descuento** (`base_mensual × meses ÷ días_ciclo × días_restantes` — el descuento de frecuencia solo premia el ciclo completo prepago, no el catch-up parcial), crea un Payment prorrateado, recalcula `billing_amount` y actualiza el pago pendiente del próximo ciclo. Frontend `payments.vue`: tabla por fase con pills mensual/trimestral/semestral (recalcula en vivo); las pills siguen editables mientras la suscripción está `pending` (el PATCH realinea el primer pago no pagado) y la frecuencia se fija al concretarse el primer pago. 13 tests en `test_hosting_multiphase.py`. |
| Platform — Hosting Subscriptions (stored-card auto-billing) | ✅ Done | Card-only recurring billing via Wompi payment sources + 3DS. Migration `0031` adds `wompi_payment_source_id`/`card_brand`/`card_last_four`/`card_exp_month`/`card_exp_year` to `HostingSubscription` and `charge_attempts`/`last_charge_error`/`next_retry_at` to `Payment`. `wompi.py` gains `get_acceptance_tokens`, `create_payment_source`, `get_payment_source`, `charge_with_payment_source` (recurrent COF/3RI). New views: `card_setup_start_view` / `card_setup_status_view` (3DS polling proxy) / `card_setup_confirm_view` / `payment_charge_stored_view`; helper `_charge_payment_with_source`. Huey task `accounts/tasks.py::auto_charge_due_subscriptions` charges due payments daily with the stored card, 3 retries 2 days apart, then suspends + notifies. Frontend `payments.vue` reworked: PSE/Nequi/Bancolombia removed (card only), 3DS card-setup modal with iframe rendering, stored-card panel, "Cambiar tarjeta", manual "Reintentar cobro". Cancellation handled off-app by email. 16 new tests in `test_subscription_card_setup.py`. Flow `platform-hosting-card-setup` registered (P1, ❌ Missing E2E spec). |
| Platform — Global Board + Profile | ✅ Done | `/platform/board` (global kanban view); `/platform/profile` (profile management) |
| Business Proposal — Contract System | ✅ Done | `CompanySettings`, `ContractTemplate`, `ProposalDocument` models (migrations 0061–0068); `contract_pdf_service.py` with contractor signature + draft mode; `ContractParamsModal.vue`, `SendDocumentsModal.vue`, `ProposalDocumentsTab.vue` admin UI; `proposal_documents_sent` email template |
| Platform — Data Model Entities | ✅ Done | `DataModelEntity` + `ProjectDataModelEntity` in accounts app; `technical_requirements_sync.py` service; `/platform/projects/[id]/data-model.vue` page; `platform-data-model.js` store; 60 backend + 26 unit + E2E tests |
| Platform — UI Terminology & UX | ✅ Done | Épica → Módulo rename; `useConfirmModal.js` refactored to promise-based API; dark mode removed from platform login/verify/complete-profile pages |
| Business Proposal — Branded Email | ✅ Done | "Correos" tab on proposal edit (negotiating/accepted/rejected): draggable sections composer, file attachments, branded preview, paginated history; `_send_composed_email()` shared service; 6 URL patterns; `EmailLog.metadata` JSONField |
| Business Proposal — Proposal Email | ✅ Done | "Enviar correo" tab on proposal edit (sent+ statuses): same composer UI, each send creates `ProposalChangeLog` with `EMAIL_SENT` change type + updates `last_activity_at`; `ProposalEmailsTab.vue` with `mode` prop |
| Blog — LinkedIn Publishing | ✅ Done | `LinkedInToken` singleton model with Fernet-encrypted OAuth tokens; `linkedin_service.py` — 3-legged OAuth flow, auto token refresh, publish/unpublish blog post summaries with cover images via LinkedIn Posts API |
| Business Proposal — Advanced Filters & Saved Tabs | ✅ Done | `useProposalFilters.js` composable (11 filter dimensions, saved tabs with localStorage, URL sync); `ProposalFilterTabs.vue` (tab bar with +, rename, delete); `ProposalFilterPanel.vue` (collapsible filter grid); single-pass client-side filtering; max 12 tabs; `selectArrowStyle.js` shared utility |
| Business Proposal — Project Schedule Tracking (Cronograma) | ✅ Done | `ProposalProjectStage` child model (design + development with `start_date`/`end_date`/`completed_at`/`warning_sent_at`/`last_overdue_reminder_at`); `ProposalStageTracker` service with `STAGE_DEFINITIONS`, `ensure_stages`, `get_or_create_stage`, `format_remaining_time`, `process(proposal)`; daily Huey task `notify_proposal_stage_deadlines` (08:30 Bogotá); 70%-elapsed warning + every-3-days overdue reminders; admin Cronograma tab (`ProjectScheduleEditor.vue` + `useStageStatus.js`); 2 PUT/POST endpoints; onboarding auto-creates rows; 2 internal email templates; 51 backend tests + 49 frontend unit tests + 6 E2E |
| Backend — Bogotá date helpers | ✅ Done | `now_bogota()`, `today_bogota()`, `to_bogota_date(dt)` in `content/utils.py`; `format_bogota_date()` accepts both `date` and `datetime` instances |
| Business Proposal — 3 New Additional Modules (Biometric, QR, Content Gen) | ✅ Done | 2026-05-07. `biometric_verification_module` (🪪, `is_invite=True`, `price_percent=0`, provider-billed integration with `invite_note`), `qr_generator_module` (🔳, `price_percent=25`, 6 items: instant generation, branded customization, dynamic editable codes, scan tracking, code library, ready-to-use cases), `content_generator_module` (✍️, `price_percent=30`, 7 items including visual editorial calendar with monthly/weekly views and scheduling/auto-publishing) added to `DEFAULT_SECTIONS` (es) and `DEFAULT_SECTIONS_EN` in `proposal_service.py`. `EXPECTED_ADDITIONAL_MODULE_ORDER` and `CALCULATOR_MODULE_IDS` constants in `tests/services/test_proposal_service.py` extended; `additionalModules` count 13 → 16 in both ES + EN; 3 new module-specific tests (`test_biometric_verification_module_is_invite_with_zero_percent`, `test_qr_generator_module_has_25_percent`, `test_content_generator_module_has_30_percent_and_calendar`). `CRITICAL_additionalModules_autoselect` prompt in `views/proposal.py` extended with detection hints for KYC/biometric, QR, and AI-content/scheduling keywords. `groupLabels` in `InvestmentCalculatorModal.vue` got 3 bilingual entries. New E2E spec `proposal-calculator-biometric-module.spec.js` (2 tests, models `is_invite` flow) + flow tag `PROPOSAL_CALCULATOR_BIOMETRIC_MODULE` (P2). No DB migration: catalog lives entirely in JSON defaults. Verification: `pytest content/tests/services/test_proposal_service.py --no-cov` 80 passed; `jest InvestmentCalculatorModal.test.js` 51 passed; biometric Playwright spec 2/2 passed. |
| Business Proposal — Real client profiles | ✅ Done | `BusinessProposal.client` FK to `accounts.UserProfile` (PROTECT, `limit_choices_to={'role':'client'}`) with migrations `0079` (schema) + `0080` (backfill dedups by normalized email, two-step placeholder generation). New `accounts/services/proposal_client_service.py` (silent variant of `onboarding.create_client` — get-or-create, update + cascade snapshots, delete-orphan with 3 guards, sync_snapshot, generate_placeholder_email, build_client_display_name). 6 FBV endpoints under `proposals/client-profiles/*` (`views/proposal_clients.py`). `ProposalListSerializer`/`ProposalDetailSerializer` expose nested `client`; `ProposalCreateUpdateSerializer` accepts `client_id`/`client_company`/`propagate_client_updates`. Email automations: `_is_unsendable_client_email` helper + `UserProfile.PLACEHOLDER_EMAIL_DOMAIN` constant + `is_email_placeholder` property gate **13 client-facing email methods** + 4 huey tasks + 2 candidate querysets. N+1 fixes via `select_related('client__user')` in 4 views. Bug fix: `respond_to_proposal` now sends `send_acceptance_confirmation` for `action='accepted'`. Frontend: `proposalClients.js` Pinia store (Options API + AbortController search), `ClientAutocomplete.vue` (debounce 200ms, keyboard nav, click-outside via VueUse, placeholder badge), `/panel/clients/` rewrite with tabs (Todos/Activos/Huérfanos) + new-client modal + trash icon for orphans, `[id]/edit.vue` and `create.vue` use `<ClientAutocomplete>` + snapshot fields + propagate checkbox. Tests: 15 service + 19 view + 10 placeholder-skip backend + 25 store frontend = **69 new tests**, zero regression. Shipped 2026-04-09. |
| Accounting Module (superuser) | ✅ Done | 2026-07-03 (#79). Double-ledger bookkeeping (`Ledger` = COMPANY / GUSTAVO / CARLOS) with a partner-split invariant (personal ledger must be 100% owner) enforced in `PartnerSplitMixin.clean()`. Models `content/models/accounting_base.py` + `IncomeRecord`/`ExpenseRecord`/`HostingRecord`/`RecurringPayment`/`AdsSpendRecord`/`PocketMovement`/`CardBalanceSnapshot`/`AccountingChangeLog`/`AccountingSettings` (9). Views `content/views/accounting.py` (dashboard/charts + per-ledger CRUD, `IsSuperUser`) + `accounting_export.py` (CSV/XLSX section export + full-year workbook). Services `accounting_service.py`, `accounting_export_service.py`, `accounting_email_service.py`, `accounting_card_reminder_service.py`. Weekly card-debt reminder Huey task (`send_card_debt_reminder`, Fridays, re-alerts every 2 days until a snapshot clears the cycle). Frontend `/panel/accounting/*` (superuser-gated) + `accounting.js` store. 12 `admin-accounting-*` E2E flows registered + covered. |
| MCP Connectors (claude.ai) | ✅ Done | 2026-07-02/03 (#77/#78). Remote Model-Context-Protocol connectors exposing panel modules to claude.ai custom connectors. Models `McpConnector` + `McpRequestLog`; token-auth JSON-RPC endpoint `content/views/mcp_blog.py` (Origin/DNS-rebinding checks, activity log); tool package `content/mcp/*` with blog/documents/proposals/diagnostics/clients/tasks/accounting connectors (seed migrations `0129`/`0131`/`0136`). Management UI `/panel/mcps` (superuser-gated: generate/rotate one-time token URL, toggle active, connection-activity feed) + `mcps.js` store + `admin-mcps` E2E flow. |
| Client-report → Documents MCP | ✅ Done | 2026-07-03 (#80). The `client-report` skill publishes session change-reports to the "Gestor Documental" MCP connector after writing the local `.md`, validating the project folder and creating/updating the document via `content/mcp/document_tools.py`. |
| Client Document-Signing Portal | ✅ Done | `/platform/documents` client portal: lists the main contract (`requires_signature`) first + annexes, downloads branded PDFs, and click-to-accept signs the main document (gated on a verified email; records `signed_at/signed_by/signature_name/signature_ip/signature_user_agent`; notifies the team). Backend `accounts/document_views.py` + `serializers_documents.py`; signing fields on `content/models/document.py`; store `platform-documents.js`. E2E: `platform-client-document-portal` / `-email-validation` / `-document-sign` (P1) + spec `platform-client-documents.spec.js`. |
| Client Email OTP Verification | ✅ Done | 6-digit email-ownership OTP required before signing. `POST /api/accounts/email/verify/request/` + `/confirm/` (`accounts/document_views.py`); `UserProfile.email_verified`/`email_verified_at` (migration `0037`); driven from the documents portal. |
| Project Scope Items | ✅ Done | `ProjectScopeItem` layer between `ProjectPhase` and `Requirement` (chain Project → ProjectPhase → ProjectScopeItem → Requirement), mirrored from accepted-proposal functional-requirement groups by `accounts/services/technical_requirements_sync.py` (re-sync overwrites proposal-authored content unless a card was taken over via `Requirement.content_overridden`). Migration `accounts/0038_project_scope_item.py`; rendered on the platform board via `GET /api/accounts/projects/<id>/scope-items/`. |
| Client-Flow Team Notifications | ✅ Done | `accounts/services/client_flow_notifications.py` fires best-effort team milestone alerts (in-app to project admins + email) on three client milestones: first platform login, email validated, document signed. Registered as system flows `admin-client-first-login-notification` / `-email-validated-notification` / `-document-signed-notification` (`expectedSpecs:0`). |
| Business Proposal — Discount Offer Send | ✅ Done | Manual "Enviar oferta de descuento" action on `/panel/proposals/:id/edit` (actions menu, shown only when `discount_percent > 0` + client email): renders the `proposal_urgency` email preview, then sends via `POST /api/proposals/:id/discount-offer/send/` → `ProposalEmailService.send_urgency_email(force=True)`. Never auto-sent. E2E flow `admin-proposal-discount-offer-send` (P2) + spec. |
| Proposal — Hour Packages + Scope Clause + Free AI Module | ✅ Done | 2026-07-07 (#91). Hour packages (July 2026 rates; "Extranjero" replaced "México"), scope clause on the PDF, free AI module with conditions. Service `hour_package_service.py`. |
| Proposal — PDF Tax Suffix | ✅ Done | 2026-07-08 (#92). "+ IVA" / "+ Tax" label on proposal PDF investment figures. |
| Panel UX — Pagination, Status Select, Inactive Clients, Seeded Filter Tabs | ✅ Done | 2026-07-09 (#93). Centered pagination, admin status select, inactive clients tab, seedable `SavedFilterTab` rows (`seed_filter_tabs` command), drag-and-drop reassign. |
| Accounting — Cuentas de Cobro, Expiry Notices, Cycle History | ✅ Done | 2026-07-09 (#94). Collection accounts (`collection_account_service.py` + PDF), hosting expiry notices, cycle history (`hosting_cycle.py`), currency inputs, KPIs. |
| Documents — UX Redesign + Emoji Rendering | ✅ Done | 2026-07-10 (#97). Gallery view, `DocumentMarkdownBody` sanitization, `:shortcode:` emoji pipeline (`shortcodes.json` + `emoji_shortcodes.py` + NotoEmoji PDF runs). |
| Infra — Unattended Frontend Rebuild Hardening | ✅ Done | 2026-07-10 (#98). Heap OOM fixed + alerting for silent rebuild failures; MCP hygiene. Huey cgroup limits raised to 4G/200%/512 tasks. |
| Proposal — PDF Redesign (Layout Engine) | ✅ Done | 2026-07-11 (#99). Layout engine + tables/badges/KPIs across comercial + técnico PDFs (`proposal_pdf_service.py` / `pdf_utils.py`). |
| Documents — Dual PDF Template | ✅ Done | 2026-07-11 (#100). Friendly/professional template selector for document PDFs (`pdf_theme.py`). |
| Emails — Tabs + Configurable Defaults | ✅ Done | 2026-07-11 (#101). Emails page tabs + configurable greeting/footer/signer defaults; Markdown toggle visibility fix. |
| Accounting — Pocket ↔ Records Bidirectional Sync | ✅ Done | 2026-07-11 (#103). Pocket movements sync both ways with income/expense records; sortable columns; hero utility chart. |
| Accounting — Card Catalog + Statements | ✅ Done | 2026-07-11 (#105). `CreditCard` + statement models (`credit_card.py`, `credit_card_statement.py`); debt computed server-side (quota − available); statement UI editing + PDF; 8-day statement reminder (`accounting_statement_reminder_service.py`). Alias normalization + reversal flag follow-up. |
| Accounting — Summary Debt + Expected Cards | ✅ Done | 2026-07-13 (#106). Card-debt and current-month expected-income cards on the accounting summary; month labels distributed across chart x-axes (#109). |
| MCP — append_document + Markdown Parser Hang Fix | ✅ Done | 2026-07-14 (#108). New `append_document` tool; markdown parser 502 hang fixed (`markdown_parser.py`). |
| Accounting — Income Liquidation (expected → liquid / lost) | ✅ Done | 2026-07-16 (#110). `IncomeRecord.Kind` expected/liquid/lost with `expected_income` FK linking a settlement to its projection; `IncomeLiquidateModal.vue` + liquidate actions on `/panel/accounting/incomes`; `lost` tab, `lost_total` + `received_pct` aggregates; expense paid/pending via `paid_amount` subquery. |
| Panel Dashboard — Multi-Module Command Center | ✅ Done | 2026-07-16 (#110). Consolidated dashboard endpoint (`build_dashboard_core` + accounting `year_totals`, `panel_dashboard_service.py`); `/panel/` page rebuilt with dashboard store + components; flows registered in flow-tags/definitions/USER_FLOW_MAP. |
| View Map — Default Filter Tabs + Settings | ✅ Done | 2026-07-16 (#110). `ViewMapSettings` (migration `0163`); default mode/filters + Configuración tab on `/panel/views`; Tarjetas filter defaults to catalog cards. |
| Accounting — Sortable Hint Icon on Table Columns | ✅ Done | 2026-07-16 (#111). `AccountingTable.vue` always renders a hint icon on sortable columns (chevron replaces it once actively sorted); 2 unit tests cover aria-sort + emit. |
| Proposal PDF — Identified Challenges Badge Panel | ✅ Done | 2026-07-16 (#112). Hand-drawn challenges box in `proposal_pdf_service.py` replaced with the branded badge-panel primitive + tier logic (11+/55−). Shipped without dedicated tests — covered by QA cycle #2 (see Testing Status). |
| QA Cycle #1 (methodology + coverage + gate + E2E) | ✅ Done | 2026-07-16 (#111). Memory bank refresh; accounting record-endpoint/hosting-cycle/change-log backend tests (views 85.7→95.2%); 4 dashboard section component specs (~36→99%); stable locators in incomes E2E; `admin-accounting-hosting-billing` (P1) + `admin-accounting-hosting-cycles` (P2) E2E flows covered. |
| QA Cycles #2–#3 (PDF challenges, accounting carry-over, statements/collections E2E) | ✅ Done | 2026-07-17 (#113). PDF challenges badge-panel coverage + remaining accounting filter branches; accounting store statement/collection/cycle operations + error paths + pocket tiebreak; statement/transaction/alias audit labels; statement-ledger + card-catalog + collections + quick-create E2E; `BaseDropdown` NuxtLink `to`-items fix; gate fixes (plain asserts for no-assertion rule, forbidden-token rename — the CI gate runs DEFAULT semantic-rules mode). |
| Accounting — Pocket Draws Against Company Utility + Liquidate Defaults | ✅ Done | 2026-07-17 (#114). Pocket egresos attributed to a partner now mirror a company-ledger expense 100% assigned to that partner (category personal), reducing liquid utility and the partner's participation (migration `0164` normalizes existing rows; expense form converts personal+pocket submissions with a warning hint; pocket modal OUT selector relabeled "Atribuir a" with prefilled attribution). Liquidate modal defaults destination to Bolsillo ProjectApp + "Registrar el día exacto de pago" toggle; income/expense `period_date` accepts full dates (YYYY-MM normalized to day 1); statements stay month-only. |
| Accounting — Stats Modals, COP Email Formatting & Weekday Date Standard | ✅ Done | 2026-07-22 (#115). `cop` template filter (`format_cop_email`) + `bogota_date` in card-reminder/hosting-expiry emails (autoescape off in TXT for the millions apostrophe); `format_bogota_date`/`format_bogota_datetime` now emit the "Jue, 16 jul 2026" weekday standard across proposal/diagnostic/accounting emails; new central `frontend/utils/formatDate.js` (formatDate/formatDateTime/formatDayMonth, Bogotá TZ via Intl, literal date-only parsing); `AccountingStatCard` clickable prop + `ExpectedIncomeDetailModal` (read-only month detail of expected company incomes). |
| Accounting — COP Formatting & Weekday Dates in Payment Notifications | ✅ Done | 2026-07-22 (#116). Phase-onboarding notifications + team payment-status email switched from en-US comma grouping and raw ISO dates to `format_cop_email` (subject + bodies) and `bogota_date` billing dates; plain-text body wrapped in autoescape off. |
| QA Campaign Round 9 (junk-only E2E flows → real coverage) | ✅ Done | 2026-07-24 (PR #125, branch `test/24072026-qa-campaign-round-8`). The rewrite work Round 8's audit deferred: converted **every** green-but-empty E2E flow into a genuinely interacting-and-asserting test. **covered 189→261, junk-only 72→0.** Closed ALL modules: P1 · layout · public · the platform module · proposal · and the whole admin module (portfolio, accounting, blog, proposal-admin, diagnostic/misc). Pattern: a real interaction where the flow acts (click/fill/submit + a gate-recognized state-change assertion — `.not.`/`toContainText`/`toHaveURL`/`toHaveValue`/`toHaveCount`/`toEqual`), and a documented `// quality: allow-no-interaction` / `allow-render-only` / `allow-fragile-selector` (reason) + concrete data assertion for genuine display/redirect/SEO/contract cases. Every touched spec left gate-0/0 (strict) and dev-server-verified (`E2E_PORT=3007`); ~15 coverage commits, the final admin batch parallelized across 3 `qa-engineer-e2e` subagents (which also found+fixed a real mock-schema bug in email-deliverability). Out of scope: **38 `missing` flows** (never had any test — a separate backlog). |
| QA Campaign Round 8 (first test-audit — junk inventory + safe cleanup) | ✅ Audit done | 2026-07-24. First-ever `test-audit` over the whole corpus (backend 5,133 funcs / unit ~4,694 / E2E ~966). Junk inventory (gate strict + flow audit): **301 no-interaction E2E, 168 flow-tag-mismatch, 164 weak-assertion, 147 duplicate_coverage, 72 junk-only flows, 38 missing**. Both approved safe-cleanup batches came back **empty on inspection**: 0 no-subject tests (no barrel/constants/re-export targets), and all 147 `duplicate_coverage` are false-positives-for-merge — 115 same generic name across *different* describe blocks (rename, not merge) + 32 cross-component structural twins (keep). No tests deleted/merged. Report: `docs/audits/test-audit-2026-07-24.md`. Deferred to Round 9: rename non-single-purpose names, rewrite no-interaction/weak E2E, reorganize the 67 misplaced `accounts` test files (gate errors invisible to CI's `content`-only backend scan). |
| Canonical Test Quality Core adopted | ✅ Done | 2026-07-24 (#124). Adopted the fleet canonical test-quality core: refreshed `scripts/quality/*` analyzers + `scripts/test_quality_gate.py` + `.testquality.yml`; pre-existing junk pinned in `.junk-baseline.json`. |
| QA Campaign Round 7 (giant components unit coverage + P3 flows) | ✅ Done | 2026-07-23 (#123). Unit coverage for the remaining giant 0% components and the last P3 user flows. |
| QA Campaign Round 6 (every partial user flow closed) | ✅ Done | 2026-07-23 (#122). Closed all 9 🟡 partial flows + all 10 ❌ P3/P4 missing flows; unit tests for the 3 giant 0% components (ProposalMultiSendModal, AccountingCardCatalog, DiagnosticDashboard). Goal met: USER_FLOW_MAP with zero 🟡/❌ rows (drag interactions documented as intentionally not asserted). |
| QA Campaign Round 5 (services tier, big components, last P2 E2E) | ✅ Done | 2026-07-23 (#121). `payment_notifications` 78→100% + `accounting_base` 81→98% (partner_attribution + 100%-owner guards) + `diagnostic_documents_service` 80→98% + `linkedin_token` 82→94% (22 tests); `AccountingMonthlyTable` + `AdSpendFormModal` 0→100% (11 tests); gate held at 0/0 (3 self-introduced issues caught and fixed in-round); E2E `platform-hosting-card-delete` covered (3 tests, definitions 2.57.0) — **no P1/P2 ❌ Missing flows remain**. Deployed to production same day (612 prerendered routes, post-deploy-check PASS=16). Carry-over: 9 partial flows, 10 P3/P4 missing, 3 giant 0% components. |
| QA Campaign Round 4 (coverage floors, MCP completion, gate zero, P2 E2E) | ✅ Done | 2026-07-22 (#120). CI coverage floors enforced (backend `--cov-fail-under=92.5`; frontend statements ≥85% / branches ≥81% via a dedicated ci.yml step — jest coverageThreshold would be swallowed by the `|| true`); MCP `document_tools` 78→98% (14 tests) + `client_flow_notifications` 80→90% (9 tests); 4 components 0→98-100% (AccountingExportButton, ExecutiveSummarySection, TimelineForm, DeliveryStructureForm, 16 tests); **gate warnings 8→0** (splits + documented test_too_long marker); E2E P2 `admin-blog-publish-mode` covered (4 tests, definitions 2.56.0, flow description corrected to hydration-time semantics). Carry-over: big 0% components (ProposalMultiSendModal 290, AccountingCardCatalog 201, DiagnosticDashboard 129, AdSpendFormModal 107, AccountingMonthlyTable 93), E2E `platform-hosting-card-delete` + 9 partials, staging fake-data refresh. |
| QA Campaign Round 3 (MCP tools, gate splits, P2 E2E) | ✅ Done | 2026-07-22 (#119). MCP `task_tools` 62→98% (20 tests), `linkedin_tools` 72→97% + `technical_document_filter` 74→92% (16 tests); 4 components 0→100% (JsonStatsPanel, AccountingSubnav, ScopeForm, DiagnosticPromptPanel, 14 tests); gate warnings 18→8 splitting ten over-asserted tests across 8 files without dropping assertions; E2E P2 closed: `admin-document-tags-manage` + `admin-standalone-email-attachments` (7 tests, definitions 2.55.0); stale skill refs fixed in both frontend-e2e-test-coverage mirrors. Carry-over: `document_tools` 78%, big 0% components (ProposalMultiSendModal, AccountingCardCatalog, DiagnosticDashboard…), 8 gate warnings, E2E `admin-blog-publish-mode` + `platform-hosting-card-delete` + 9 partials. |
| QA Campaign Round 2 (carry-over coverage + gate + P2 E2E) | ✅ Done | 2026-07-22 (#118). MCP `proposal_tools` 57→97% (16 tests) + `accounting_tools` 60→94% (14 tests); 8 components 0→100% (WebAppDiagnostic badge/scope/purpose/delivery + DocumentsToolbar/DocumentSearchInput/ViewModuleGrid/ViewMapLegend, 27 tests); gate warnings 32→18 (freeze_time ×9, mock verification ×3, specific asserts ×2); E2E P2 gaps closed: `admin-document-rename`, `admin-document-delete`, `admin-document-folder-manage` (13 tests, definitions 2.53.0); pre-existing strict-mode flake fixed in `admin-proposal-delete.spec.js`. Carry-over: MCP task/linkedin/document tools + technical_document_filter, remaining 0% components, 18 gate warnings (too_many_assertions splits), E2E P2 tags-manage/blog-publish-mode/standalone-email-attachments/hosting-card-delete + 9 partials. |
| QA Campaign Round 1 (seven-skill phased cycle) | ✅ Done | 2026-07-22 (#117). Memory bank refresh post-#116; fake accounting data models the #114 pocket-draw rule (partner draws mirror company expenses, exact-date liquidations, 3 invariant tests); MCP `diagnostic_tools` 29→92% + `statement_tools` 50→92% (40 tests); six 0% shared frontend files → 100% (42 tests: diagnosticNextAction, proposalJsonStats, hourPackagePricing, proposalModuleSelectionStorage, downloadFile, usePanelRefresh); gate warnings 50→32 via `freeze_time` in 18 nondeterministic tests; E2E P1 gap `admin-document-send-email` closed (3 tests, flow-definitions 2.51.0) + `admin-proposal-send` map reconciled to Covered. Carry-over: MCP proposal/accounting/task tools, 0% components (WebAppDiagnostic/documents/views clusters), 32 gate warnings, P2 documents E2E cluster. |
| Modal search results — floating layer and visible bulk scope | ✅ Done | 2026-08-26. Shared `BaseFloatingListbox` removes clipping and double scroll from client, project and linked-income selectors in Contable and Documentos. `BulkAssignModal` grows to a bounded desktop height, shows five results plus the four-record review, and remains full-screen on narrow viewports. Unit, focused E2E and five-viewport responsive checks are green. |

---

## 2. Known Issues & Technical Debt

| Issue | Priority | Notes |
|-------|----------|-------|
| RSP-F5-01 — Physical responsive certification | P0 | Backlog. Ejecutar el guion de 12 módulos en hardware real para los cinco perfiles y conservar evidencia de touch, teclado, barras/safe-area, portátil y monitor wide. Playwright no cierra este punto. |
| RSP-F5-02 — Fleet responsive standard drift | P1 | Backlog externo en `vps-ops-toolkit`. El proyecto referencia `/responsive-module`, el canónico aún `/responsive-pass`; sincronizar y sembrar/medir el ledger sin degradar la copia vigente. |
| RSP-F5-03 — Legacy overlay migration | P1 | Backlog. Clasificar cada `fixed inset-0`, migrar modales/drawers a primitives y justificar por escrito los workspaces/overlays públicos legítimos antes de declarar PA-45 completamente cerrada. |
| RSP-F5-04 — Responsive E2E and ledger alignment | P1 | Backlog. Sustituir o versionar los cinco Playwright projects globales, llevar cada checklist a un E2E calificable y mapear módulos responsivos como `clients` a flows `admin`/`platform` medibles por QA. |
| Credential rotation needed | High | MySQL password, email password, SECRET_KEY, CallMeBot key exposed in git history (see `docs/deployment-guide.md`) |
| Port 3000 squatted by `kore_project` | Medium | A Windsurf terminal respawns `kore_project` Next.js on port 3000. Workaround: run Nuxt on 3001 with `E2E_PORT=3001`. Tracked as `KNOWN-001` in `error-documentation.md`. |
| Large service files | Medium | `proposal_service.py`, `proposal_pdf_service.py`, `proposal_email_service.py`, and `pdf_utils.py` remain large enough to justify future splitting |
| Large view file | Medium | `views/proposal.py` remains a very large FBV module and could benefit from submodule extraction |
| Single Django app for content | Low | All proposal/blog/portfolio/contact models in `content` app; consider splitting if scope grows |
| One bank transfer paying several expenses books N pocket movements | Low | The balance is right, the ledger's shape is not: an extract showing one debit appears as three. **Evaluated and deliberately out of scope for the abono v1** — expenses have no `kind`, so no expected/liquid split and nothing to impute against; full rationale, cost breakdown and the cheap first step (`ExpenseRecord.pocket_movement` OneToOne→FK, nothing else) in `tasks/active_context.md`. Revisit when expected expenses start being recorded. |

---

## 3. Testing Status

| Suite | Location | Approximate Count | Status |
|-------|----------|-------------------|--------|
| Backend (pytest) | `backend/content/tests/` + `backend/accounts/tests/` + `backend/projectapp/tests/` + `backend/tests/` | 256 test files / 5,133 test funcs (content 185/3,883, accounts 67/1,153, projectapp 2/30, root 2/67) | Active |
| Frontend Unit (Jest) | `frontend/test/` | 373 test files (~4,694 cases) | Active |
| Frontend E2E (Playwright) | `frontend/e2e/` | 219 spec files (~966 tests) across admin, auth, blog, layout, proposal, public, platform, visual | Active |
| Quality Gate | `scripts/test_quality_gate.py` | Re-run to confirm current score | Active |

---

## 4. Documentation Status

| Document | Location | Status |
|----------|----------|--------|
| Responsive Standard | `docs/RESPONSIVE_STANDARD.md` | ✅ Phase 0 complete |
| Responsive Acceptance | `docs/methodology/responsive-acceptance.md` | ✅ Mandatory, automated and reviewed semiannually |
| Product Requirements | `docs/methodology/product_requirement_docs.md` | ✅ Initialized |
| Architecture | `docs/methodology/architecture.md` | ✅ Initialized |
| Technical | `docs/methodology/technical.md` | ✅ Initialized |
| Task Plan | `tasks/tasks_plan.md` | ✅ Initialized |
| Active Context | `tasks/active_context.md` | ✅ Initialized |
| Error Documentation | `docs/methodology/error-documentation.md` | ✅ Initialized |
| Lessons Learned | `docs/methodology/lessons-learned.md` | ✅ Initialized |
| Codex Methodology Guide | `docs/CODEX_METHODOLOGY_GUIDE.md` | ✅ Complete |
| Codex Setup Quickstart | `docs/CODEX_SETUP.md` | ✅ Complete |
| Codex Migration Map | `docs/CODEX_MIGRATION_MAP.md` | ✅ Complete |
| Deployment Guide | `docs/deployment-guide.md` | ✅ Complete |
| Testing Quality Standards | `docs/TESTING_QUALITY_STANDARDS.md` | ✅ Complete |
| User Flow Map | `docs/USER_FLOW_MAP.md` | ✅ Complete |
| E2E Flow Definitions | `frontend/e2e/flow-definitions.json` | ✅ Complete |
| README | `README.md` | ✅ Complete |
| CI Workflow | `.github/workflows/ci.yml` | ✅ Complete |
| Nginx Config | `scripts/nginx/projectapp.conf` | ✅ Complete |
| Systemd Services | `scripts/systemd/` | ✅ Complete |

---

## 5. Potential Improvements

1. **Split large files** — proposal views/services and the shared PDF utility layer are still large enough to benefit from decomposition
2. **API versioning** — no versioning strategy currently
3. **Rate limiting** — no rate limiting on public endpoints
4. **Caching layer** — Redis available but no application-level caching implemented
5. **WebSocket notifications** — real-time alerts instead of polling
6. **Multi-tenant support** — currently single-company; could generalize for SaaS
7. **Codex docs drift guard** — add a lightweight check ensuring native skill inventory and sensitive-skill policy stay in sync with Codex methodology docs
