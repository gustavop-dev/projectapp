# Task Plan — ProjectApp

## 1. Feature Status

| Feature | Status | Details |
|---------|--------|---------|
| Business Proposal — Core Models | ✅ Done | BusinessProposal, ProposalSection, ProposalAlert, RequirementGroup/Item |
| Business Proposal — Public View | ✅ Done | Fullscreen horizontal scroll, 12 section components, GSAP animations |
| Business Proposal — Admin CRUD | ✅ Done | Create, edit, send, duplicate, JSON import, section editor, defaults, alerts |
| Business Proposal — Email System | ✅ Done | 73 content email templates (37 HTML + 36 TXT), automated reminders, cooldown, pause, admin notifications |
| Business Proposal — Initial Email Refresh + PDF Attached | ✅ Done | New `email_intro` TextField on `BusinessProposal` (migration `0117`); `proposal_sent_client` HTML/TXT rewritten with phase card, executive summary, payment breakdown iterating `paymentOptions`, signature; `_build_initial_email_context` reads `INVESTMENT.paymentOptions` + `TIMELINE.totalDuration` from sections via `ProposalSection.SectionType` enum; new shared `_attach_commercial_pdf` helper auto-attaches the commercial PDF (reused by `send_acceptance_confirmation`, fixing prior `Propuesta_comercial_` casing inconsistency); registry entry updated with `closing` editable field + new variables; admin form gets `email_intro` textarea (`data-testid=edit-email-intro`); 5 new tests. |
| Business Proposal — Multi-Proposal Send (Single Email) | ✅ Done | New `POST /api/proposals/<id>/send-multi/` view + `?client_id=` filter on `list_proposals`. `ProposalEmailService.send_multi_proposal_to_client(proposals)` builds one email with N PDF attachments + N `EmailLog` rows sharing a `group_uuid`. `ProposalService.send_multi_proposals(proposals)` applies per-proposal side effects (draft→sent + reminders, expired→reopen + extend `expires_at`, sent/viewed/negotiating→reset timers) using the `BusinessProposal.Status` enum; validates same-client. New `proposal_multi_sent_client` template (HTML/TXT) iterates phases ("Propuesta N de M"). Modal `ProposalMultiSendModal.vue` (BaseModal) lists same-client proposals grouped by status (Borradores / Enviadas-Vistas-Negociación / Expiradas), pre-selects current, max 10, error surface, reusable `frontend/utils/formatMoney.js`. New action item in `ProposalActionsModal`. 6 new pytest cases; `admin-proposal-multi-send` flow registered (P1, ❌ Missing E2E spec). |
| Business Proposal — Analytics | ✅ Done | View tracking, section time, heat score, session tracking, engagement signals |
| Business Proposal — PDF | ✅ Done | ReportLab generation, downloadable from proposal page |
| Business Proposal — Share Links | ✅ Done | UUID share links with independent tracking |
| Business Proposal — Investment Calculator | ✅ Done | Interactive modal for payment options |
| Business Proposal — Client Responses | ✅ Done | Accept, reject (with reason), negotiate |
| Business Proposal — Expiration | ✅ Done | Auto-expire via daily Huey cron task |
| Business Proposal — Change Log | ✅ Done | Full audit trail (20+ change types) |
| Business Proposal — Email Deliverability | ✅ Done | Dashboard with send/delivery/bounce rates |
| Business Proposal — Email Templates Editor | ✅ Done | View, edit, preview, reset email content |
| Business Proposal — Default Config | ✅ Done | Per-language default section templates |
| Business Proposal — Clients List | ✅ Done | Real `accounts.UserProfile` client entities power `/panel/clients/`, proposal autocomplete, orphan cleanup, and snapshot sync back to linked proposals |
| Portfolio Works — Public | ✅ Done | Listing and detail with bilingual structured JSON |
| Portfolio Works — Admin CRUD | ✅ Done | Create, edit, delete, duplicate, cover image upload, JSON import |
| Blog — Public | ✅ Done | Listing with featured hero, categories, pagination, detail with JSON/HTML |
| Blog — Admin CRUD | ✅ Done | Create, edit, delete, duplicate, cover image upload, calendar view, JSON import |
| Blog — Sitemap | ✅ Done | Sitemap data endpoint + XML rendering |
| Portfolio — Sitemap | ✅ Done | Sitemap data endpoint |
| Contact Form | ✅ Done | Public submission with budget ranges, email notification |
| Landing Pages (3) | ✅ Done | Web design, software, apps — with animations |
| Home Page | ✅ Done | Marketing page with GSAP animations |
| About Us Page | ✅ Done | Company information page |
| Internationalization (i18n) | ✅ Done | EN/ES with prefix routing, lazy loading, geo-detection |
| Admin Auth Middleware | ✅ Done | Session/CSRF check, redirect to Django admin login |
| CI/CD Pipeline | ✅ Done | GitHub Actions: pytest, Jest, Playwright (5 shards), quality gate |
| Codex Ecosystem Methodology | ✅ Done | Codex-first runtime documented and implemented: `AGENTS.md` hierarchy, native repo skills in `.agents/skills/`, project config in `.codex/config.toml`, canonical guide + quickstart, legacy compatibility notes |
| Deployment (Production) | ✅ Done | Gunicorn + Nginx + systemd, documented in deployment-guide.md |
| WhatsApp Notifications | ✅ Done | CallMeBot API integration |
| Database Backups | ✅ Done | django-dbbackup with rotation |
| Query Profiling (Silk) | ✅ Done | Optional, env-gated |
| Platform — Auth & Onboarding | ✅ Done | JWT login, OTP verify, complete-profile, middleware gating |
| Platform — Dashboard | ✅ Done | Admin KPI cards + recent clients; Client profile summary |
| Platform — Projects & Kanban | ✅ Done | Project CRUD, detail hub, 3-column kanban board, drag & drop, comments |
| Platform — Client Management | ✅ Done | Admin invite, list, detail, edit, deactivate, reactivate |
| Platform — Sidebar & Layout | ✅ Done | Collapsible sidebar, mobile drawer, theme toggle, role-based nav |
| Platform — E2E Coverage | ✅ Done | Platform flows are registered and covered across auth, dashboard, projects, kanban, clients, collection accounts, data model, notifications, payments, and related routes |
| Document System — Model + Admin CRUD | ✅ Done | `Document` model (uuid, title, slug, status, language, cover_type); panel pages (index, create, edit); `documents.js` store |
| Document System — Folders & Tags | ✅ Done | `DocumentFolder` + `DocumentTag` models (migration `0086`); CRUD FBV views + 8 URL patterns; `document_folders.js` + `document_tags.js` stores; `FolderSidebar`, `TagFilterChips`, `TagSelector`, `FolderManagerModal`, `TagManagerModal` components; `documentTagColors.js` shared util; `index.vue` 2-col layout + `create.vue`/`edit.vue` selectors; `?folder=<id|none>` + `?tags=<ids>` OR filtering; 25 backend + ~24 unit + 3 E2E tests |
| Admin Panel — Internal Kanban Task Board | ✅ Done | `Task` model (status/priority TextChoices, position, assignee FK SET_NULL, due_date); migration `0087_task.py`; 5 FBV endpoints (`tasks/`, `tasks/create/`, `tasks/<id>/update/`, `tasks/<id>/reorder/`, `tasks/<id>/delete/`); `TaskListSerializer` (assignee_name, is_overdue via context today) + `TaskCreateUpdateSerializer`; `tasks.js` Pinia Options API store (fetchTasks, createTask, updateTask, moveTask, deleteTask, replaceTaskInPlace); `TaskCard.vue` (priority badge, due_date red if overdue), `TaskColumn.vue` (vuedraggable cross-column DnD), `TaskFormModal.vue`; `/panel/tareas` page with `useConfirmModal`; panel nav entry "Tareas" / "Kanban" as first section; 11 backend tests + 9 frontend unit tests + 2 E2E specs; `ADMIN_KANBAN_TASKS` flow tag registered |
| Business Proposal — Admin UX Improvements | ✅ Done | Inline "days" number input beside `datetime-local` on create + edit pages (bidirectional sync, time-preserving); `@temp.example.com` domain bypass in `validate_email_domain_mx` + `jsonForm` default; fixed bottom-right save toast with Tailwind `<Transition>`, `clearTimeout` guard, 5 s auto-dismiss |
| Business Proposal — Value Added Modules Section | ✅ Done | New `section_type = 'value_added_modules'` (migration `0102`). `ValueAddedModules.vue` public component renders card grid of included-free modules (icon, title, justification, description). Seeded from `functional_requirements.manual_module` group for existing proposals. `test_value_added_modules.py` covers default schema; `ValueAddedModules.test.js` created (not yet run locally). |
| Business Proposal — ROI Projection Section | ✅ Done | New `section_type = 'roi_projection'` (migration `0118`) sitting at `order=4` between `conversion_strategy` and `investment` so the sponsor sees quantified outcomes (visualizations/day, sponsor reach, MRR, payback, year-1 revenue, scenarios cons/realistic/optimistic) **before** the price ask. `RoiProjection.vue` public component (cards grid + scenarios block + CTA banner). Admin form added to `SectionEditor.vue` with drag-drop KPIs and scenarios + 3 cases in `sectionEditorUtils.js`. **Web-only**: intentionally absent from `SECTION_RENDERERS` so it skips the PDF; PDF loop now also drops the TOC entry for sections without a renderer (consistent with `proposal_summary` and `process_methodology`). `linkify` hardened to escape HTML by default (whitelist of `<b>/<strong>/<i>/<em>/<br>`) before injecting into `v-html`. Migration backfilled an empty `is_enabled=False` row in every existing proposal + bumped existing orders >=4 by +1. **Caveat**: backfill silently no-op'd because `ProposalDefaultConfig (es)` had a stale 16-section list — fixed via shell using the canonical `DEFAULT_SECTIONS` import. Pre-existing order collision `final_note=14, next_steps=14, technical_document=15` resolved as 14/15/16 in source + prod. Albunmanía proposal (id=91) populated with the financial copy ($122M investment, payback 5–9 mo, 6 KPIs, 3 scenarios). Tests: 11 backend pytest + 14 frontend unit (8 sectionEditorUtils + 6 linkify) + 2 E2E specs. `EXPECTED_DEFAULT_SECTION_COUNT` 16 → 17. Flow `proposal-roi-projection` (P1) registered. |
| Diagnostic Public View — PDF Download, Share & Dark Mode | ✅ Done | `DownloadDiagnosticPdfButton.vue` (FAB, calls `GET /api/diagnostics/public/{uuid}/pdf/`); `ShareDiagnosticButton.vue` (FAB, copy-link + Web Share API, bilingual, slide-up modal); `useDiagnosticDarkMode.js` (module-level singleton, persists to localStorage, applies `data-theme` to `[data-diagnostic-wrapper]`); `diagnostic/[uuid]/index.vue` redesigned with esmerald brand tokens, `rounded-3xl` cards, theme toggle; public section components updated with brand tokens; new `DiagnosticPdfService` backend service mirroring `ProposalPdfService`; `download_public_diagnostic_pdf` view (`GET /api/diagnostics/public/<uuid>/pdf/`, AllowAny, gated by `PUBLIC_VISIBLE_STATUSES`). |
| MarkdownAttachmentModal — Diagnostic Template Copy | ✅ Done | `showDiagnosticTemplates` Boolean prop on `MarkdownAttachmentModal.vue`; 3 copy buttons (Aplicación → Técnico → Anexo) above markdown textarea; lazy fetch via `GET /api/diagnostic-templates/<slug>/`, per-slug `reactive({})` cache; `copyFeedbackTimer` tracked + cleared on close/unmount; `DiagnosticEmailsTab.vue` passes `show-diagnostic-templates`. 3 new unit tests pass. |
| Business Proposal — Value Added Modules Order Fix | ✅ Done | `proposal_service.py` seed reordered: `functional_requirements` → order 9, `value_added_modules` → order 10. Migration `0103_swap_value_added_modules_order.py`. Public view hides `value_added_modules` when no `module_ids` resolve against FR groups. `useSellerPrompt.js` updated with correct index and narrative position. |
| Business Proposal — Calculator Module Reorder + Seller Prompt Auto-Select | ✅ Done | `additionalModules` array reordered in ES + EN to business-priority order (payments → AI → marketing → UX). `reports_alerts_module` renamed to include WhatsApp; 6th item (💚) added to `items` list. `_seller_prompt` in `proposal.py` gained `CRITICAL_additionalModules_autoselect` key with keyword-to-module detection mapping (DIAN → `integration_electronic_invoicing`, PSE/Wompi → `integration_regional_payments`, etc.) so Claude auto-sets `default_selected: true` + adapts copy when requirements mention a module. `CRITICAL_functionalRequirements` count made count-agnostic. Tests in `test_proposal_service.py` updated (new order + 6-item assertion). |
| Business Proposal — Personal Slug URL | ✅ Done | `slug = SlugField(unique=True, blank=True, max_length=120)` on `BusinessProposal`. Auto-generated from `ProposalDefaultConfig.default_slug_pattern` (e.g. `{client_name}`) via `render_slug_pattern`; collision-free via `resolve_unique_slug` (1 DB query via `__startswith` + Python set). Migrations `0104` (populate+dedupe+`unique=True`) + `0105` (`default_slug_pattern` field on `ProposalDefaultConfig`). New URL `proposals/by-slug/<slug>/` delegates to `_serve_public_proposal` helper; UUID route fully preserved for backward compatibility. `'slug'` added to `tracked_fields` for audit log. Frontend: `frontend/utils/slugify.js` (`toSlug()` NFD normalize); `fetchPublicProposal` UUID/slug branch detection; editable slug input on proposal edit General tab (Save + Regenerate); live pattern preview in `ProposalDefaultsPanel.vue`. `public_url` property uses `slug or uuid`. Tests: `TestBusinessProposalSlug` (auto-gen, collision suffix, preserved) + `TestBusinessProposalPublicUrl` updated + 4 new by-slug API tests. |
| Documents Tab Reorganization & doc_refs Email Attachment | ✅ Done | Documents tabs are now pure document management (list/download/upload). "Enviar al cliente" sections removed from both Proposal and Diagnostic Documents tabs. Diagnostic MD templates (Aplicación/Técnico/Anexo) moved from Proposal aside → Diagnostic "Documentos" tab. New `AttachFromDocumentsModal.vue` (shared) + `useDocRefsAttachment.js` composable added. Both Emails tabs gained "Adjuntar desde Documentos" button that passes `doc_refs` JSON field to the send endpoints. Backend: new `_doc_refs.py` shared helper (`parse_doc_refs_field` + `DocRefError`), `_resolve_proposal_doc_refs` and `_resolve_diagnostic_doc_refs` resolvers with pre-batched ORM queries. `TEMPLATES_DIR` made public in `diagnostic_template.py`. Deleted: `SendDocumentsModal.vue`, `ProposalDiagnosticTemplatesSection.vue`, `SendDiagnosticDocumentsModal.vue` + their tests. Orphaned store actions `sendProposalDocuments`/`sendDiagnosticAttachments` + backend endpoints still exist — cleanup pending. |
| Diagnostic Templates Exposure in Proposal Tab | ✅ Superseded | Superseded by "Documents Tab Reorganization" (Apr 22). `ProposalDiagnosticTemplatesSection.vue` deleted; MD templates are now listed in `DiagnosticDocumentsTab.vue` (the correct owner). `diagnostic-templates/` API endpoints remain in use. |
| Admin Panel — Unified Defaults Page | ✅ Done | `frontend/pages/panel/defaults.vue` shell with mode switch (Propuesta/Diagnóstico) synced to `?mode=` query param via `computed`. Two panel components extracted: `ProposalDefaultsPanel.vue` (6 tabs) + `DiagnosticDefaultsPanel.vue` (5 tabs) under `components/panel/defaults/`. Both loaded via `defineAsyncComponent`. Old routes (`/panel/proposals/defaults`, `/panel/diagnostics/defaults`) become redirect stubs preserving `?tab=`. Sidebar renamed to "Defaults" and moved to top of Sales. All internal links updated. No backend changes. |
| Diagnostic Analytics — Full Parity with Proposal Analytics | ✅ Done | `diagnostic_analytics` view fully rewritten: engagement score (0–100), funnel with drop-off %, device breakdown (tablet/mobile/desktop via UA), sessions history (50 rows, no view_mode column), activity timeline from `DiagnosticChangeLog`, skipped sections, global comparison, 3 N+1 patterns eliminated, legacy compat keys removed, `WebAppDiagnostic.Status` enum replaces stringly-typed checks. New `export_diagnostic_analytics_csv` view + `GET /api/diagnostics/<id>/analytics/csv/` route. `DiagnosticAnalytics.vue` rewritten to ~963 LOC with 12 collapsible sections mirroring proposal tab (minus view_mode comparison, share links, technical engagement). New `test_diagnostic_analytics.py` (10 tests). `/simplify` pass verified no remaining N+1 or quality issues. |
| Diagnostic Markdown-to-PDF Email Attachment | ✅ Done | When diagnostic is in `negotiating` status, "Crear documento desde markdown" button appears in `DiagnosticEmailsTab.vue`. `MarkdownAttachmentModal.vue` (refactored to `endpoint: String` prop — moved to `components/` root for cross-context reuse) generates a branded PDF via `POST /api/diagnostics/<id>/email/markdown-attachment/` → `DocumentPdfService.generate_from_markdown()` → returns inline PDF; seller previews in iframe, then attaches as a `File` object to the email composer. Shared backend helper `content/views/_email_attachment.py` (functions `render_markdown_pdf_response` + `inline_pdf_response`) removes duplication between proposal and diagnostic attachment views. New frontend utilities: `useMarkdownAttachmentHandler.js` composable + `emailAttachments.js` validator + `proposals_constants.js` frozen enum. |
| Web App Diagnostics — Default Config | ✅ Done | `DiagnosticDefaultConfig` singleton-per-language model (`backend/content/models/diagnostic_default_config.py`, migration `0101`) holding `sections_json`, `payment_initial_pct=60`/`payment_final_pct=40` (with `clean()` sum-to-100 guard), `default_currency` (COP/USD), `default_investment_amount`, `default_duration_label`, `expiration_days=21`/`reminder_days=7`/`urgency_reminder_days=14`. `DiagnosticDefaultConfigSerializer` validates `sections_json` shape (`{section_type, title, order, content_json}`) and re-checks the payment sum. `diagnostic_service.py` exposes `DEFAULT_PAYMENT_INITIAL_PCT=60` etc. + `get_default_config(lang)` / `get_default_section_specs(lang)`; `create_diagnostic` reads the config once and forwards it to `seed_sections(diagnostic, config=...)` — no duplicate query. 2 admin endpoints: `GET/PUT /api/diagnostics/defaults/?lang=` and `POST /api/diagnostics/defaults/reset/`. Frontend page `panel/diagnostics/defaults.vue` (5 tabs: General + Sections + Emails-link + Prompt-placeholder + JSON) reuses `usePanelToast`, `useConfirmModal`, `ResponsiveTabs`, `<PanelToast />`. Pinia actions `fetchDiagnosticDefaults`/`saveDiagnosticDefaults`/`resetDiagnosticDefaults` (whitelisted general fields). Header link added to `panel/diagnostics/index.vue`. Tests: 8 model + 16 view backend (24 green) + 6 frontend store unit (jest run pending local install). Side effect: a misplaced `penal_clause_value` field was moved back to `ConfidentialityParamsSerializer`. |
| Web App Diagnostics — Full Module | ✅ Done | **JSON-section architecture** (rewrite Apr 16, 2026): `WebAppDiagnostic` model + new `DiagnosticSection` (8 section_types: `purpose`/`radiography`/`categories`/`delivery_structure`/`executive_summary`/`cost`/`timeline`/`scope`, `content_json`, `visibility` ∈ initial/final/both), `DiagnosticChangeLog`, `DiagnosticViewEvent`, `DiagnosticSectionView`. Migrations `0094` (schema) + `0095` (seed existing rows) + `0096` (drop legacy `DiagnosticDocument`). `content/seeds/diagnostic_template.py` → `default_sections()` seeds the 8-section + 14-category payload via `bulk_create`. Service (`diagnostic_service.py`): `create_diagnostic`, `seed_sections`, `reset_section`, `transition_status` (writes `STATUS_CHANGE` log), `visible_sections(phase)`, `register_view`, `log_change`, `build_render_context`. Email service (`diagnostic_email_service.py`): `send_initial_to_client`, `send_final_to_client`, `send_custom_email` + rate-limited composer. Serializers: `DiagnosticSectionSerializer` + `DiagnosticChangeLogSerializer`; `DiagnosticDetailSerializer` memoizes `build_render_context` per instance and slices prefetched `change_logs` in Python; `PublicDiagnosticSerializer` whitelists `render_context` via `PUBLIC_RENDER_CONTEXT_KEYS`. Views: 18 FBVs under `/api/diagnostics/` — CRUD, sections CRUD (single + bulk + reset), activity log list/create, analytics aggregate, send-initial/mark-in-analysis/send-final, attachments CRUD + send, email composer (send/defaults/history/attach-confidentiality), public retrieve/track/track-section/respond. `_ensure_view_event` dedups session rows; `_send_and_transition` returns `(ok, email_ok)` so the UI surfaces email failures. `get_client_ip` hoisted to `content/utils.py`. Admin page `panel/diagnostics/[id]/edit.vue` has 5 base + 2 status-gated tabs: `general` → `[emails]` (≥SENT) → `[documents]` (≥NEGOTIATING) → `sections` → `prompt` → `json` → `activity` → `analytics`. **UI refinements (Apr 20)**: sticky title format changed to `$X.XXX.XXX COP` via `formatInvestment` (parity with business proposal); `size_category` removed from General tab form; Sections/Activity/Analytics tabs expanded to full page width (no `max-w-4xl`); NDA checkbox added to Correos tab (`attachConfidentiality` flag → backend `_parse_diagnostic_email` generates and attaches the confidentiality PDF or returns 400). The previous consolidated `technical` tab (Pricing + Radiografía sub-tabs) was removed on 2026-04-18; pricing fields live in General, radiography is edited as a regular section, and legacy deep-links (`?tab=pricing|radiography|technical`) redirect to `general`/`sections`. Admin components (`components/WebAppDiagnostic/admin/`): `DiagnosticSectionEditor.vue` + 8 per-type forms under `admin/sections/`, `DiagnosticPromptPanel.vue` + `PromptEditor.vue`, `DiagnosticActivityTab.vue`, `DiagnosticAnalytics.vue`. Shared `usePromptState({storageKey,defaultPrompt})` factory (`composables/usePromptState.js`) + `useDiagnosticCommercialPrompt`/`useDiagnosticTechnicalPrompt`. Public components: 8 section components + `SectionHeader.vue` + `DiagnosticIndex.vue` (sidebar index — hamburger toggle, backdrop blur, floating panel, numbered badges, visited checkmarks) under `components/WebAppDiagnostic/public/`. Public page (`pages/diagnostic/[uuid]/index.vue`): `layout: false`; no default Nuxt header; sections rendered via `COMPONENTS` map; `DiagnosticIndex` sidebar replaces the former pill nav; `visitedIds` ref + `markSectionVisited` helper track visited sections; per-section dwell time tracked with `session_id`, final row flushed via `navigator.sendBeacon` on unmount. `diagnostics_constants.js` exports `DIAGNOSTIC_STATUS`/`STATUS_META`/`SECTION_VISIBILITY`/`VISIBILITY_OPTIONS`/`SEVERITY_LEVELS`/`severityLevelClass`/`ACTIVITY_CHANGE_TYPES`; `arrToText`/`textToArr` re-imported from `BusinessProposal/admin/sectionEditorUtils.js`. Pinia store actions: `updateSection`/`bulkUpdateSections`/`resetSection`/`fetchActivity`/`logActivity`/`fetchAnalytics`/`trackSectionView`. Legacy `.md` templates retained under `templates/diagnostics/_legacy/` (unused at runtime). Tests: 31 backend (18 in `test_web_app_diagnostic.py` + 13 in `test_diagnostic_attachments_and_emails.py`) + 41 frontend (31 store + 10 section-utils roundtrip) — all green. |
| Document System — PDF Generation | ✅ Done | `document_pdf_service.py`, `markdown_parser.py`, `pdf_utils.py` (shared utilities). Branded markdown→PDF is live and reused across proposal/diagnostic attachments and the `/platform/documents` client signing portal. |
| Platform — Quick Access | ✅ Done | `/platform/access` (admin-only); `production_url`, `staging_url`, `admin_url`, `repository_url`, `admin_username`, `admin_password_encrypted` on `Project`; `GET /api/accounts/projects/access/`; Fernet encryption via `accounts/services/credential_cipher.py`; `accounts/admin.py` for data entry; `cryptography>=42,<46` added to requirements; `PROJECT_ACCESS_CIPHER_KEY` env var |
| Panel — Admins Management | ✅ Done | `panel/admins/index.vue` + `panel_admins.js` store |
| Panel — Dedicated Login | ✅ Done | `panel/login.vue` page |
| Platform — Bug Reports | ✅ Done | `platform-bug-reports.js`; global `/platform/bugs` + per-project `/platform/projects/[id]/bugs`; `test_bug_reports.py` |
| Platform — Change Requests | ✅ Done | `platform-change-requests.js`; global `/platform/changes` + per-project `/platform/projects/[id]/changes`; `test_change_requests.py` |
| Platform — Deliverables | ✅ Done | `platform-deliverables.js`; global `/platform/deliverables` + per-project `/platform/projects/[id]/deliverables`; `test_deliverables.py` |
| Platform — Notifications | ✅ Done | `platform-notifications.js`; `/platform/notifications`; `test_notifications.py` |
| Platform — Payments | ✅ Done | `platform-payments.js`; global `/platform/payments` + per-project `/platform/projects/[id]/payments`; `test_payments.py` |
| Platform — Hosting multi-fase con prorrateo | ✅ Done | El hosting de un proyecto factura la **suma de todas sus fases iniciadas** a una frecuencia única (antes: una sola propuesta). Migración `0032` añade `ProjectPhase.hosting_activated_at`. Servicio nuevo `accounts/services/hosting_billing.py` (`phase_billing_amount`, `project_billing_amount`, `prorated_amount`, `phase_tiers`). `_create_subscription_multi_phase` reemplaza a `_create_subscription_from_proposal`: la activación cobra solo fases ya iniciadas (`hosting_start_date` nulo o ≤ hoy); las fases con fecha futura entran después prorrateadas. Tarea Huey `_onboard_due_phases` (dentro de `auto_charge_due_subscriptions`): cuando la fecha de una fase llega, calcula el prorrateo **a precio full sin descuento** (`base_mensual × meses ÷ días_ciclo × días_restantes` — el descuento de frecuencia solo premia el ciclo completo prepago, no el catch-up parcial), crea un Payment prorrateado, recalcula `billing_amount` y actualiza el pago pendiente del próximo ciclo. Frontend `payments.vue`: tabla por fase con pills mensual/trimestral/semestral (recalcula en vivo); las pills siguen editables mientras la suscripción está `pending` (el PATCH realinea el primer pago no pagado) y la frecuencia se fija al concretarse el primer pago. 13 tests en `test_hosting_multiphase.py`. |
| Platform — Hosting Subscriptions (stored-card auto-billing) | ✅ Done | Card-only recurring billing via Wompi payment sources + 3DS. Migration `0031` adds `wompi_payment_source_id`/`card_brand`/`card_last_four`/`card_exp_month`/`card_exp_year` to `HostingSubscription` and `charge_attempts`/`last_charge_error`/`next_retry_at` to `Payment`. `wompi.py` gains `get_acceptance_tokens`, `create_payment_source`, `get_payment_source`, `charge_with_payment_source` (recurrent COF/3RI). New views: `card_setup_start_view` / `card_setup_status_view` (3DS polling proxy) / `card_setup_confirm_view` / `payment_charge_stored_view`; helper `_charge_payment_with_source`. Huey task `accounts/tasks.py::auto_charge_due_subscriptions` charges due payments daily with the stored card, 3 retries 2 days apart, then suspends + notifies. Frontend `payments.vue` reworked: PSE/Nequi/Bancolombia removed (card only), 3DS card-setup modal with iframe rendering, stored-card panel, "Cambiar tarjeta", manual "Reintentar cobro". Cancellation handled off-app by email. 16 new tests in `test_subscription_card_setup.py`. Flow `platform-hosting-card-setup` registered (P1, ❌ Missing E2E spec). |
| Platform — Global Board + Profile | ✅ Done | `/platform/board` (global kanban view); `/platform/profile` (profile management) |
| Business Proposal — Contract System | ✅ Done | `CompanySettings`, `ContractTemplate`, `ProposalDocument` models (migrations 0061–0068); `contract_pdf_service.py` with contractor signature + draft mode; `ContractParamsModal.vue`, `SendDocumentsModal.vue`, `ProposalDocumentsTab.vue` admin UI; `proposal_documents_sent` email template |
| Platform — Data Model Entities | ✅ Done | `DataModelEntity` + `ProjectDataModelEntity` in accounts app; `technical_requirements_sync.py` service; `/platform/projects/[id]/data-model.vue` page; `platform-data-model.js` store; 60 backend + 26 unit + E2E tests |
| Platform — UI Terminology & UX | ✅ Done | Épica → Módulo rename; `useConfirmModal.js` refactored to promise-based API; dark mode removed from platform login/verify/complete-profile pages |
| Business Proposal — Branded Email | ✅ Done | "Correos" tab on proposal edit (negotiating/accepted/rejected): draggable sections composer, file attachments, branded preview, paginated history; `_send_composed_email()` shared service; 6 URL patterns; `EmailLog.metadata` JSONField |
| Business Proposal — Proposal Email | ✅ Done | "Enviar correo" tab on proposal edit (sent+ statuses): same composer UI, each send creates `ProposalChangeLog` with `EMAIL_SENT` change type + updates `last_activity_at`; `ProposalEmailsTab.vue` with `mode` prop |
| Blog — LinkedIn Publishing | ✅ Done | `LinkedInToken` singleton model with Fernet-encrypted OAuth tokens; `linkedin_service.py` — 3-legged OAuth flow, auto token refresh, publish/unpublish blog post summaries with cover images via LinkedIn Posts API |
| Business Proposal — Advanced Filters & Saved Tabs | ✅ Done | `useProposalFilters.js` composable (11 filter dimensions, saved tabs with localStorage, URL sync); `ProposalFilterTabs.vue` (tab bar with +, rename, delete); `ProposalFilterPanel.vue` (collapsible filter grid); single-pass client-side filtering; max 12 tabs; `selectArrowStyle.js` shared utility |
| Business Proposal — Project Schedule Tracking (Cronograma) | ✅ Done | `ProposalProjectStage` child model (design + development with `start_date`/`end_date`/`completed_at`/`warning_sent_at`/`last_overdue_reminder_at`); `ProposalStageTracker` service with `STAGE_DEFINITIONS`, `ensure_stages`, `get_or_create_stage`, `format_remaining_time`, `process(proposal)`; daily Huey task `notify_proposal_stage_deadlines` (08:30 Bogotá); 70%-elapsed warning + every-3-days overdue reminders; admin Cronograma tab (`ProjectScheduleEditor.vue` + `useStageStatus.js`); 2 PUT/POST endpoints; onboarding auto-creates rows; 2 internal email templates; 51 backend tests + 49 frontend unit tests + 6 E2E |
| Backend — Bogotá date helpers | ✅ Done | `now_bogota()`, `today_bogota()`, `to_bogota_date(dt)` in `content/utils.py`; `format_bogota_date()` accepts both `date` and `datetime` instances |
| Business Proposal — 3 New Additional Modules (Biometric, QR, Content Gen) | ✅ Done | 2026-05-07. `biometric_verification_module` (🪪, `is_invite=True`, `price_percent=0`, provider-billed integration with `invite_note`), `qr_generator_module` (🔳, `price_percent=25`, 6 items: instant generation, branded customization, dynamic editable codes, scan tracking, code library, ready-to-use cases), `content_generator_module` (✍️, `price_percent=30`, 7 items including visual editorial calendar with monthly/weekly views and scheduling/auto-publishing) added to `DEFAULT_SECTIONS` (es) and `DEFAULT_SECTIONS_EN` in `proposal_service.py`. `EXPECTED_ADDITIONAL_MODULE_ORDER` and `CALCULATOR_MODULE_IDS` constants in `tests/services/test_proposal_service.py` extended; `additionalModules` count 13 → 16 in both ES + EN; 3 new module-specific tests (`test_biometric_verification_module_is_invite_with_zero_percent`, `test_qr_generator_module_has_25_percent`, `test_content_generator_module_has_30_percent_and_calendar`). `CRITICAL_additionalModules_autoselect` prompt in `views/proposal.py` extended with detection hints for KYC/biometric, QR, and AI-content/scheduling keywords. `groupLabels` in `InvestmentCalculatorModal.vue` got 3 bilingual entries. New E2E spec `proposal-calculator-biometric-module.spec.js` (2 tests, models `is_invite` flow) + flow tag `PROPOSAL_CALCULATOR_BIOMETRIC_MODULE` (P2). No DB migration: catalog lives entirely in JSON defaults. Verification: `pytest content/tests/services/test_proposal_service.py --no-cov` 80 passed; `jest InvestmentCalculatorModal.test.js` 51 passed; biometric Playwright spec 2/2 passed. |
| Business Proposal — Real client profiles | ✅ Done | `BusinessProposal.client` FK to `accounts.UserProfile` (PROTECT, `limit_choices_to={'role':'client'}`) with migrations `0079` (schema) + `0080` (backfill dedups by normalized email, two-step placeholder generation). New `accounts/services/proposal_client_service.py` (silent variant of `onboarding.create_client` — get-or-create, update + cascade snapshots, delete-orphan with 3 guards, sync_snapshot, generate_placeholder_email, build_client_display_name). 6 FBV endpoints under `proposals/client-profiles/*` (`views/proposal_clients.py`). `ProposalListSerializer`/`ProposalDetailSerializer` expose nested `client`; `ProposalCreateUpdateSerializer` accepts `client_id`/`client_company`/`propagate_client_updates`. Email automations: `_is_unsendable_client_email` helper + `UserProfile.PLACEHOLDER_EMAIL_DOMAIN` constant + `is_email_placeholder` property gate **13 client-facing email methods** + 4 huey tasks + 2 candidate querysets. N+1 fixes via `select_related('client__user')` in 4 views. Bug fix: `respond_to_proposal` now sends `send_acceptance_confirmation` for `action='accepted'`. Frontend: `proposalClients.js` Pinia store (Options API + AbortController search), `ClientAutocomplete.vue` (debounce 200ms, keyboard nav, click-outside via VueUse, placeholder badge), `/panel/clients/` rewrite with tabs (Todos/Activos/Huérfanos) + new-client modal + trash icon for orphans, `[id]/edit.vue` and `create.vue` use `<ClientAutocomplete>` + snapshot fields + propagate checkbox. Tests: 15 service + 19 view + 10 placeholder-skip backend + 25 store frontend = **69 new tests**, zero regression. Shipped 2026-04-09. |
| Accounting Module (superuser) | ✅ Done | 2026-07-03 (#79). Double-ledger bookkeeping (`Ledger` = COMPANY / GUSTAVO / CARLOS) with a partner-split invariant (personal ledger must be 100% owner) enforced in `PartnerSplitMixin.clean()`. Models `content/models/accounting_base.py` + `IncomeRecord`/`ExpenseRecord`/`HostingRecord`/`RecurringPayment`/`AdsSpendRecord`/`PocketMovement`/`CardBalanceSnapshot`/`AccountingChangeLog`/`AccountingSettings` (9). Views `content/views/accounting.py` (dashboard/charts + per-ledger CRUD, `IsSuperUser`) + `accounting_export.py` (CSV/XLSX section export + full-year workbook). Services `accounting_service.py`, `accounting_export_service.py`, `accounting_email_service.py`, `accounting_card_reminder_service.py`. Weekly card-debt reminder Huey task (`send_card_debt_reminder`, Fridays, re-alerts every 2 days until a snapshot clears the cycle). Frontend `/panel/accounting/*` (superuser-gated) + `accounting.js` store. 12 `admin-accounting-*` E2E flows registered + covered. |
| MCP Connectors (claude.ai) | ✅ Done | 2026-07-02/03 (#77/#78). Remote Model-Context-Protocol connectors exposing panel modules to claude.ai custom connectors. Models `McpConnector` + `McpRequestLog`; token-auth JSON-RPC endpoint `content/views/mcp_blog.py` (Origin/DNS-rebinding checks, activity log); tool package `content/mcp/*` with blog/documents/proposals/diagnostics/clients/tasks/accounting connectors (seed migrations `0129`/`0131`/`0136`). Management UI `/panel/mcps` (superuser-gated: generate/rotate one-time token URL, toggle active, connection-activity feed) + `mcps.js` store + `admin-mcps` E2E flow. |
| Client-report → Documents MCP | ✅ Done | 2026-07-03 (#80). The `client-report` skill publishes session change-reports to the "Gestor de Documentos" MCP connector after writing the local `.md`, validating the project folder and creating/updating the document via `content/mcp/document_tools.py`. |
| Client Document-Signing Portal | ✅ Done | `/platform/documents` client portal: lists the main contract (`requires_signature`) first + annexes, downloads branded PDFs, and click-to-accept signs the main document (gated on a verified email; records `signed_at/signed_by/signature_name/signature_ip/signature_user_agent`; notifies the team). Backend `accounts/document_views.py` + `serializers_documents.py`; signing fields on `content/models/document.py`; store `platform-documents.js`. E2E: `platform-client-document-portal` / `-email-validation` / `-document-sign` (P1) + spec `platform-client-documents.spec.js`. |
| Client Email OTP Verification | ✅ Done | 6-digit email-ownership OTP required before signing. `POST /api/accounts/email/verify/request/` + `/confirm/` (`accounts/document_views.py`); `UserProfile.email_verified`/`email_verified_at` (migration `0037`); driven from the documents portal. |
| Project Scope Items | ✅ Done | `ProjectScopeItem` layer between `ProjectPhase` and `Requirement` (chain Project → ProjectPhase → ProjectScopeItem → Requirement), mirrored from accepted-proposal functional-requirement groups by `accounts/services/technical_requirements_sync.py` (re-sync overwrites proposal-authored content unless a card was taken over via `Requirement.content_overridden`). Migration `accounts/0038_project_scope_item.py`; rendered on the platform board via `GET /api/accounts/projects/<id>/scope-items/`. |
| Client-Flow Team Notifications | ✅ Done | `accounts/services/client_flow_notifications.py` fires best-effort team milestone alerts (in-app to project admins + email) on three client milestones: first platform login, email validated, document signed. Registered as system flows `admin-client-first-login-notification` / `-email-validated-notification` / `-document-signed-notification` (`expectedSpecs:0`). |
| Business Proposal — Discount Offer Send | ✅ Done | Manual "Enviar oferta de descuento" action on `/panel/proposals/:id/edit` (actions menu, shown only when `discount_percent > 0` + client email): renders the `proposal_urgency` email preview, then sends via `POST /api/proposals/:id/discount-offer/send/` → `ProposalEmailService.send_urgency_email(force=True)`. Never auto-sent. E2E flow `admin-proposal-discount-offer-send` (P2) + spec. |
| Proposal — Hour Packages + Scope Clause + Free AI Module | ✅ Done | 2026-07-07 (#91). Hour packages (July 2026 rates; "Extranjero" replaced "México"), scope clause on the PDF, free AI module with conditions. Service `hour_package_service.py`. |
| Proposal — PDF Tax Suffix | ✅ Done | 2026-07-08 (#92). "+ IVA" / "+ Tax" label on proposal PDF investment figures. |
| Panel UX — Pagination, Status Select, Inactive Clients, Seeded Filter Tabs | ✅ Done | 2026-07-09 (#93). Centered pagination, admin status select, inactive clients tab, seedable `SavedFilterTab` rows (`seed_filter_tabs` command), drag-and-drop reassign. |
| Accounting — Cuentas de Cobro, Expiry Notices, Cycle History | ✅ Done | 2026-07-09 (#94). Collection accounts (`collection_account_service.py` + PDF), hosting expiry notices, cycle history (`hosting_cycle.py`), currency inputs, KPIs. |
| Documents — UX Redesign + Emoji Rendering | ✅ Done | 2026-07-10 (#97). Gallery view, `DocumentMarkdownBody` sanitization, `:shortcode:` emoji pipeline (`shortcodes.json` + `emoji_shortcodes.py` + NotoEmoji PDF runs). |
| Infra — Unattended Frontend Rebuild Hardening | ✅ Done | 2026-07-10 (#98). Heap OOM fixed + alerting for silent rebuild failures; MCP hygiene. Huey cgroup limits raised to 4G/200%/512 tasks. |
| Proposal — PDF Redesign (Layout Engine) | ✅ Done | 2026-07-11 (#99). Layout engine + tables/badges/KPIs across comercial + técnico PDFs (`proposal_pdf_service.py` / `pdf_utils.py`). |
| Documents — Dual PDF Template | ✅ Done | 2026-07-11 (#100). Friendly/professional template selector for document PDFs (`pdf_theme.py`). |
| Emails — Tabs + Configurable Defaults | ✅ Done | 2026-07-11 (#101). Emails page tabs + configurable greeting/footer/signer defaults; Markdown toggle visibility fix. |
| Accounting — Pocket ↔ Records Bidirectional Sync | ✅ Done | 2026-07-11 (#103). Pocket movements sync both ways with income/expense records; sortable columns; hero utility chart. |
| Accounting — Card Catalog + Statements | ✅ Done | 2026-07-11 (#105). `CreditCard` + statement models (`credit_card.py`, `credit_card_statement.py`); debt computed server-side (quota − available); statement UI editing + PDF; 8-day statement reminder (`accounting_statement_reminder_service.py`). Alias normalization + reversal flag follow-up. |
| Accounting — Summary Debt + Expected Cards | ✅ Done | 2026-07-13 (#106). Card-debt and current-month expected-income cards on the accounting summary; month labels distributed across chart x-axes (#109). |
| MCP — append_document + Markdown Parser Hang Fix | ✅ Done | 2026-07-14 (#108). New `append_document` tool; markdown parser 502 hang fixed (`markdown_parser.py`). |
| Accounting — Income Liquidation (expected → liquid / lost) | ✅ Done | 2026-07-16 (#110). `IncomeRecord.Kind` expected/liquid/lost with `expected_income` FK linking a settlement to its projection; `IncomeLiquidateModal.vue` + liquidate actions on `/panel/accounting/incomes`; `lost` tab, `lost_total` + `received_pct` aggregates; expense paid/pending via `paid_amount` subquery. |
| Panel Dashboard — Multi-Module Command Center | ✅ Done | 2026-07-16 (#110). Consolidated dashboard endpoint (`build_dashboard_core` + accounting `year_totals`, `panel_dashboard_service.py`); `/panel/` page rebuilt with dashboard store + components; flows registered in flow-tags/definitions/USER_FLOW_MAP. |
| View Map — Default Filter Tabs + Settings | ✅ Done | 2026-07-16 (#110). `ViewMapSettings` (migration `0163`); default mode/filters + Configuración tab on `/panel/views`; Tarjetas filter defaults to catalog cards. |
| Accounting — Sortable Hint Icon on Table Columns | ✅ Done | 2026-07-16 (#111). `AccountingTable.vue` always renders a hint icon on sortable columns (chevron replaces it once actively sorted); 2 unit tests cover aria-sort + emit. |
| Proposal PDF — Identified Challenges Badge Panel | ✅ Done | 2026-07-16 (#112). Hand-drawn challenges box in `proposal_pdf_service.py` replaced with the branded badge-panel primitive + tier logic (11+/55−). Shipped without dedicated tests — covered by QA cycle #2 (see Testing Status). |
| QA Cycle #1 (methodology + coverage + gate + E2E) | ✅ Done | 2026-07-16 (#111). Memory bank refresh; accounting record-endpoint/hosting-cycle/change-log backend tests (views 85.7→95.2%); 4 dashboard section component specs (~36→99%); stable locators in incomes E2E; `admin-accounting-hosting-billing` (P1) + `admin-accounting-hosting-cycles` (P2) E2E flows covered. |
| QA Cycles #2–#3 (PDF challenges, accounting carry-over, statements/collections E2E) | ✅ Done | 2026-07-17 (#113). PDF challenges badge-panel coverage + remaining accounting filter branches; accounting store statement/collection/cycle operations + error paths + pocket tiebreak; statement/transaction/alias audit labels; statement-ledger + card-catalog + collections + quick-create E2E; `BaseDropdown` NuxtLink `to`-items fix; gate fixes (plain asserts for no-assertion rule, forbidden-token rename — the CI gate runs DEFAULT semantic-rules mode). |
| Accounting — Pocket Draws Against Company Utility + Liquidate Defaults | ✅ Done | 2026-07-17 (#114). Pocket egresos attributed to a partner now mirror a company-ledger expense 100% assigned to that partner (category personal), reducing liquid utility and the partner's participation (migration `0164` normalizes existing rows; expense form converts personal+pocket submissions with a warning hint; pocket modal OUT selector relabeled "Atribuir a" with prefilled attribution). Liquidate modal defaults destination to Bolsillo ProjectApp + "Registrar el día exacto de pago" toggle; income/expense `period_date` accepts full dates (YYYY-MM normalized to day 1); statements stay month-only. |
| Accounting — Stats Modals, COP Email Formatting & Weekday Date Standard | ✅ Done | 2026-07-22 (#115). `cop` template filter (`format_cop_email`) + `bogota_date` in card-reminder/hosting-expiry emails (autoescape off in TXT for the millions apostrophe); `format_bogota_date`/`format_bogota_datetime` now emit the "Jue, 16 jul 2026" weekday standard across proposal/diagnostic/accounting emails; new central `frontend/utils/formatDate.js` (formatDate/formatDateTime/formatDayMonth, Bogotá TZ via Intl, literal date-only parsing); `AccountingStatCard` clickable prop + `ExpectedIncomeDetailModal` (read-only month detail of expected company incomes). |
| Accounting — COP Formatting & Weekday Dates in Payment Notifications | ✅ Done | 2026-07-22 (#116). Phase-onboarding notifications + team payment-status email switched from en-US comma grouping and raw ISO dates to `format_cop_email` (subject + bodies) and `bogota_date` billing dates; plain-text body wrapped in autoescape off. |
| QA Campaign Round 3 (MCP tools, gate splits, P2 E2E) | ✅ Done | 2026-07-22 (#119). MCP `task_tools` 62→98% (20 tests), `linkedin_tools` 72→97% + `technical_document_filter` 74→92% (16 tests); 4 components 0→100% (JsonStatsPanel, AccountingSubnav, ScopeForm, DiagnosticPromptPanel, 14 tests); gate warnings 18→8 splitting ten over-asserted tests across 8 files without dropping assertions; E2E P2 closed: `admin-document-tags-manage` + `admin-standalone-email-attachments` (7 tests, definitions 2.55.0); stale skill refs fixed in both frontend-e2e-test-coverage mirrors. Carry-over: `document_tools` 78%, big 0% components (ProposalMultiSendModal, AccountingCardCatalog, DiagnosticDashboard…), 8 gate warnings, E2E `admin-blog-publish-mode` + `platform-hosting-card-delete` + 9 partials. |
| QA Campaign Round 2 (carry-over coverage + gate + P2 E2E) | ✅ Done | 2026-07-22 (#118). MCP `proposal_tools` 57→97% (16 tests) + `accounting_tools` 60→94% (14 tests); 8 components 0→100% (WebAppDiagnostic badge/scope/purpose/delivery + DocumentsToolbar/DocumentSearchInput/ViewModuleGrid/ViewMapLegend, 27 tests); gate warnings 32→18 (freeze_time ×9, mock verification ×3, specific asserts ×2); E2E P2 gaps closed: `admin-document-rename`, `admin-document-delete`, `admin-document-folder-manage` (13 tests, definitions 2.53.0); pre-existing strict-mode flake fixed in `admin-proposal-delete.spec.js`. Carry-over: MCP task/linkedin/document tools + technical_document_filter, remaining 0% components, 18 gate warnings (too_many_assertions splits), E2E P2 tags-manage/blog-publish-mode/standalone-email-attachments/hosting-card-delete + 9 partials. |
| QA Campaign Round 1 (seven-skill phased cycle) | ✅ Done | 2026-07-22 (#117). Memory bank refresh post-#116; fake accounting data models the #114 pocket-draw rule (partner draws mirror company expenses, exact-date liquidations, 3 invariant tests); MCP `diagnostic_tools` 29→92% + `statement_tools` 50→92% (40 tests); six 0% shared frontend files → 100% (42 tests: diagnosticNextAction, proposalJsonStats, hourPackagePricing, proposalModuleSelectionStorage, downloadFile, usePanelRefresh); gate warnings 50→32 via `freeze_time` in 18 nondeterministic tests; E2E P1 gap `admin-document-send-email` closed (3 tests, flow-definitions 2.51.0) + `admin-proposal-send` map reconciled to Covered. Carry-over: MCP proposal/accounting/task tools, 0% components (WebAppDiagnostic/documents/views clusters), 32 gate warnings, P2 documents E2E cluster. |

---

## 2. Known Issues & Technical Debt

| Issue | Priority | Notes |
|-------|----------|-------|
| Credential rotation needed | High | MySQL password, email password, SECRET_KEY, CallMeBot key exposed in git history (see `docs/deployment-guide.md`) |
| Port 3000 squatted by `kore_project` | Medium | A Windsurf terminal respawns `kore_project` Next.js on port 3000. Workaround: run Nuxt on 3001 with `E2E_PORT=3001`. Tracked as `KNOWN-001` in `error-documentation.md`. |
| Large service files | Medium | `proposal_service.py`, `proposal_pdf_service.py`, `proposal_email_service.py`, and `pdf_utils.py` remain large enough to justify future splitting |
| Large view file | Medium | `views/proposal.py` remains a very large FBV module and could benefit from submodule extraction |
| Single Django app for content | Low | All proposal/blog/portfolio/contact models in `content` app; consider splitting if scope grows |

---

## 3. Testing Status

| Suite | Location | Approximate Count | Status |
|-------|----------|-------------------|--------|
| Backend (pytest) | `backend/content/tests/` + `backend/accounts/tests/` + `backend/tests/` | 254 test files (content 184, accounts 66, root/project 4) | Active |
| Frontend Unit (Jest) | `frontend/test/` | 364 test files | Active |
| Frontend E2E (Playwright) | `frontend/e2e/` | 215 spec files across admin, auth, blog, layout, proposal, public, platform, visual | Active |
| Quality Gate | `scripts/test_quality_gate.py` | Re-run to confirm current score | Active |

---

## 4. Documentation Status

| Document | Location | Status |
|----------|----------|--------|
| Product Requirements | `docs/methodology/product_requirement_docs.md` | ✅ Initialized |
| Architecture | `docs/methodology/architecture.md` | ✅ Initialized |
| Technical | `docs/methodology/technical.md` | ✅ Initialized |
| Task Plan | `tasks/tasks_plan.md` | ✅ Initialized |
| Active Context | `tasks/active_context.md` | ✅ Initialized |
| Error Documentation | `docs/methodology/error-documentation.md` | ✅ Initialized |
| Lessons Learned | `docs/methodology/lessons-learned.md` | ✅ Initialized |
| Codex Methodology Guide | `docs/CODEX_METHODOLOGY_GUIDE.md` | ✅ Complete |
| Codex Setup Quickstart | `docs/CODEX_SETUP.md` | ✅ Complete |
| Codex Migration Map | `docs/CODEX_MIGRATION_MAP.md` | ✅ Complete |
| Deployment Guide | `docs/deployment-guide.md` | ✅ Complete |
| Testing Quality Standards | `docs/testing-quality-standards.md` | ✅ Complete |
| User Flow Map | `docs/USER_FLOW_MAP.md` | ✅ Complete |
| E2E Flow Definitions | `frontend/e2e/flow-definitions.json` | ✅ Complete |
| README | `README.md` | ✅ Complete |
| CI Workflow | `.github/workflows/ci.yml` | ✅ Complete |
| Nginx Config | `scripts/nginx/projectapp.conf` | ✅ Complete |
| Systemd Services | `scripts/systemd/` | ✅ Complete |

---

## 5. Potential Improvements

1. **Split large files** — proposal views/services and the shared PDF utility layer are still large enough to benefit from decomposition
2. **API versioning** — no versioning strategy currently
3. **Rate limiting** — no rate limiting on public endpoints
4. **Caching layer** — Redis available but no application-level caching implemented
5. **WebSocket notifications** — real-time alerts instead of polling
6. **Multi-tenant support** — currently single-company; could generalize for SaaS
7. **Codex docs drift guard** — add a lightweight check ensuring native skill inventory and sensitive-skill policy stay in sync with Codex methodology docs
